---
type: concept
status: draft
area: [architecture, cnn, sampling, aliasing]
aliases: [Pooling 与下采样, CNN 混叠]
node_id: ARCH-05
prerequisites: ["[[离散卷积、互相关与边界约定]]", "[[局部连接、参数共享与平移等变性]]", "[[数列、极限与完备性的直觉]]"]
related: ["[[堆叠卷积、感受野与有效感受野]]", "[[CNN 阶段、残差块与深度可分离卷积]]"]
sources: ["[[S-2019-Zhang-AntiAliased-CNN]]", "[[S-2016-Goodfellow-Bengio-Courville-Deep-Learning]]", "[[S-2023-Zhang-Lipton-Li-Smola-D2L]]"]
exercises: ["[[习题 - 池化、下采样、混叠与不变性边界]]"]
solutions: ["[[解答 - 池化、下采样、混叠与不变性边界]]"]
figure: "[[00-知识库管理/_assets/figures/architecture/fig-pooling-sampling-aliasing-v1.svg]]"
created: 2026-08-24
updated: 2026-08-29
---

# 池化、下采样、混叠与不变性边界

> [!abstract] 本节主问题
> Pooling 包含“窗口内汇总”和“改变采样格”两个动作。Stride/downsampling 减少分辨率与计算，却降低可表示的最高频率；若事先不抑制高频，不同信号可能得到相同低分辨率样本，即 aliasing。局部 max/average 可带来一定稳定性，但不自动给出严格全局平移不变。

## 课程位置与两遍学习路线

- **承接什么：** ARCH-03 证明 stride-1 共享卷积在相容定义域上平移等变，ARCH-04 则把 stride 放进输出 shape；现在要解释 $S>1$ 为什么不只是“少算几个位置”；
- **本页解决什么：** 把 pooling 拆成局部汇总、滤波与抽样，构造两个不同高分辨率信号在低分辨率上完全相同的反例；
- **后续为何需要：** ARCH-06 的 jump 递推会使用 stride，ARCH-07 的 stage transition 会把下采样当作分辨率—计算—信息三方交换。

**第一遍只识别信息丢失。** 手算直接隔点取样，看到两个输入变成同一输出；再手算 box low-pass，理解抗混叠为什么必须发生在 decimation 以前。

**第二遍重建频域与实验合同。** 区分整数平移、subpixel shift、边界与任务指标，说明低通的代价，并设计同时报告 consistency、accuracy 和系统成本的实验。

### 问题链

1. Pooling 中“窗口汇总”和“改变采样格”分别做了什么？
2. 为什么下采样会降低可表示的最高频率？
3. 怎样构造两个不同信号在 stride-2 后完全相同？
4. 为什么 low-pass 必须放在抽样以前，抽样以后再处理已经太晚？
5. 局部稳定、平移等变与全局不变为何是三种不同主张？
6. Anti-aliasing 的结构理由为何不能直接推出任务准确率提高？

> [!check] 第一遍停靠线
> 若你能复算 $D_2u_{\mathrm{hi}}=D_2u_{\mathrm{dc}}$，并解释 box filter 如何在抽样前区分二者，就可以进入 ARCH-06。Nyquist 的频域推导和真实任务协议留到第二遍。

## 符号与对象账本

| 对象 | 数学身份 | AI 中的身份 | 不能偷换成 |
|---|---|---|---|
| $\rho$ | 局部非线性汇总 | max/average pooling window | decimation 本身 |
| $H_h$ | 滤波算子 | fixed/learned blur 或 stride-1 conv | 理想砖墙低通 |
| $D_S$ | $[D_Sz][m]=z[mS]$ | stride-$S$ 抽样 | 可逆 reshape |
| $u_{\mathrm{hi}},u_{\mathrm{dc}}$ | 两个高分辨率探针 | 高频与常值 feature | 数据集准确率证据 |
| $y=D_SH_hx$ | 低分辨率表示 | stage transition 输出 | 原输入的无损副本 |
| $r_{\mathrm{cons}}$ | 变换一致性指标 | shift consistency | 分类正确率 |

### 贯穿算例 $\mathcal C_\square$：一个不可逆的抽样碰撞

在 ARCH-01—04 的局部对比卡上增加两个长度为 8 的采样探针：

$$
u_{\mathrm{hi}}=(1,-1,1,-1,1,-1,1,-1),
\qquad
u_{\mathrm{dc}}=(1,1,1,1,1,1,1,1).
$$

直接保留偶数位置时，

$$
D_2u_{\mathrm{hi}}=(1,1,1,1)
=D_2u_{\mathrm{dc}}.
$$

两个原信号已经发生 exact collision；任何只看下采样结果的后续深网都无法判断输入是哪一个。现在先用周期 two-tap box filter

$$
(H_{\mathrm{box}}u)[n]=\frac{u[n]+u[n+1]}2
$$

再抽样，则

$$
D_2H_{\mathrm{box}}u_{\mathrm{hi}}=(0,0,0,0),
\qquad
D_2H_{\mathrm{box}}u_{\mathrm{dc}}=(1,1,1,1).
$$

低通避免了这一次碰撞，但也删除了 alternating detail；“更可区分某些低频对象”与“保留全部信息”不能同时宣称。

## 核心公式七问：先滤波，再抽样

$$
y[m]=(D_SH_hx)[m]=(h*x)[mS].
$$

| 问题 | 本式的回答 |
|---|---|
| 目的 | 在降低采样率前抑制新 Nyquist 上限以上的分量，减少频谱折叠 |
| 对象 | $H_h$ 在原网格上滤波，$D_S$ 只保留每隔 $S$ 个位置；复合顺序不可交换 |
| 来路 | 采样频谱是原频谱的缩放副本之和；副本重叠时产生不可逆 aliasing |
| 步骤 | 先对邻域加权求和得到 $z=H_hx$，再取 $y[m]=z[mS]$；若先抽样，已碰撞信号无法由后处理分开 |
| 读法 | 先删除低分辨率无法表示的高频，再改变采样格 |
| 检查 | $S=1$ 时不发生 decimation；$h=\delta$ 时退化为直接抽样；alternating/DC 探针应给出上述碰撞与分离 |
| 去路 | ARCH-06 用 $S$ 更新 jump，ARCH-07 把低通/stride 放进 stage 预算，ViT patch embedding 也需同类审计 |

## 一、三个操作先拆开

对序列 $x$：

- local summary：$z[i]=\rho(x[i:i+K])$，$\rho$ 可为 max/mean/$L^p$；
- filtering：$z=h*x$；
- decimation：$y[m]=z[mS]$。

Average pooling 可看成 box low-pass 后抽样；max pooling 是非线性 summary 后抽样；stride convolution 是 learned filtering 与 decimation 的组合。讨论 aliasing 时不能只说 “pooling”。

## 二、Max 与 Average 的最小手算

取 $x=[1,4,2,3]$，window 2、stride 2：

$$
maxpool(x)=[4,3],\qquad avgpool(x)=[2.5,2.5].
$$

输入右移一格并左补 0 得 $x'=[0,1,4,2]$：

$$
maxpool(x')=[1,4],\qquad avgpool(x')=[0.5,3].
$$

它们并非原输出的简单平移。窗口格发生相位错位，这是 stride 破坏一般整数平移等变的最小例子。

## 三、采样定理给出的必要警告

连续/离散频域的精确表述需固定规范；核心事实是：按因子 $S$ 下采样后，新 Nyquist 上限缩小为原来的 $1/S$。原信号中超过上限的频率会折叠进低频，造成 aliasing。

对 $y[m]=x[mS]$，频谱的缩放副本相加。若它们重叠，无法从 $y$ 唯一恢复 $x$。因此经典策略是先用 low-pass $h$：

$$
y[m]=(h*x)[mS].
$$

理想砖墙低通通常不可有限支持实现，真实 blur filter 是精度、局部性和成本的折衷。

## 四、一个离散混叠反例

在整数点采样，$\cos((\omega+2\pi k)n)=\cos(\omega n)$。进一步按 2 抽样：

$$
\cos((\pi-\omega)2m)=\cos(2\pi m-2\omega m)=\cos(2\omega m).
$$

某些原本不同的频率在低采样率上完全相同。网络随后再深也无法从已混同的样本中恢复被删除的信息，除非有额外先验或旁路。

## 五、Pooling 与 Invariance 的准确说法

若一个小 pattern 在同一 pooling window 内轻微移动且 max 值不变，max pooling 输出可能不变；但跨越窗口边界就会改变。Average pooling 对小扰动可能较平滑，却也不严格不变。

真正的全局不变读出可用对合法群轨道求和/平均：

$$
r(x)=\sum_{g\in G} f(T_gx),
$$

在有限群且 $f$ 等变/索引匹配时可证明不变。局部 pooling 不是这一证明的自动替代品。

## 六、Anti-Aliasing 的架构位置

一个常见模块把 stride 操作拆成：

1. stride-1 nonlinear/filtering operation；
2. fixed or learned low-pass；
3. stride-$S$ decimation。

[[S-2019-Zhang-AntiAliased-CNN]]研究了在 CNN 下采样路径加入 blur 的具体方案，并报告若干模型/任务上的 shift consistency 和 accuracy 变化。课程采用 signal-processing 理由与实验结果，但不把“某 blur filter 普遍更准”写成定理。

## 七、低通也有代价

- 高频边缘、纹理和小目标可能被削弱；
- 较宽 filter 增加计算与边界范围；
- 固定 blur 未必匹配 learned feature 的频谱；
- nonlinearity 可在低通后重新产生高频；
- 对 detection/segmentation，空间精度与 invariance 存在张力。

因此 anti-aliasing 是有明确机制的设计选择，不是免费补丁。

## 八、Shape 与成本

单轴 pooling 输出仍用

$$
L_{out}=\left\lfloor\frac{L+P_l+P_r-K}{S}\right\rfloor+1.
$$

Pooling 通常无可学习参数，但有算术、activation 与 IO。下采样后后续层的空间元素缩小约 $S^2$（二维），这往往是其系统收益的主体，而非 pooling 本身的计算便宜。

## 九、图：直接抽样与先低通

先看图回答：为什么两个相邻像素的平移会导致深网输出大变，而问题不一定出在 convolution 本身？

![[00-知识库管理/_assets/figures/architecture/fig-pooling-sampling-aliasing-v1.svg|900]]

> [!figure] 图 40.1-05　下采样混叠、低通修正与 pooling 不变性边界
> 左栏用高频曲线显示直接隔点取样可出现伪低频；中栏显示低通后抽样更稳定但牺牲细节；右栏拆开 local summary、decimation 与 global invariance。来源：依据采样理论及 Zhang 2019 独立绘制；由 [[00-知识库管理/_labs/code/plot_architecture_convolution_advanced_v1.py]] 生成。

**怎样读图**：红点不是原曲线的忠实低分辨率版本；比较右栏时要分开结构性质和特定数据集 accuracy。

**图没有证明什么**：示意曲线不证明某个有限 blur 的最优性，也不证明低通对任何视觉任务都提高准确率。

## 十、实验审计协议

至少报告：原图/平移图的 logits consistency、任务指标、平移范围（整数或 subpixel）、padding、下采样位置、blur kernel、训练是否重做、成本变化。只报告“更稳”而不报告 accuracy，或只报告 accuracy 而不测 shift consistency，都不足以支持机制主张。

## 十一、常见错误

1. 把 max pooling 当严格平移不变；
2. 把 stride 只当减小 shape，忽略采样格；
3. 认为 average pooling 自动是理想 anti-aliasing；
4. 看到 aliasing 就声称所有任务都应更强低通；
5. 混淆 integer shift、subpixel shift 与 crop；
6. 只比 FLOPs，漏掉低通和后续 activation IO；
7. 把一篇论文的经验提升升级为普遍结论。

## 十二、AI 中的连接

- CNN stage transition：stride conv/pool 是主要分辨率切换；
- ViT patch embedding：大 stride patchification 也是强下采样；
- audio/time series：decimation 前抗混叠更加直接；
- multi-scale detection：保留高分辨率旁路对小目标重要；
- generative decoder：upsampling 后也可能出现 spectral artifacts，但机制与 decimation 不完全相同。

## 十三、回顾与掌握标准

> [!summary]
> - pooling = summary + 可能的 decimation；
> - downsampling 降低 Nyquist 上限，高频未滤除会 alias；
> - anti-aliasing 先低通再抽样，同时有信息代价；
> - 局部稳定、等变和全局不变不是同一性质；
> - 机制、任务指标和系统成本必须共同验证。

## 十四、练习与独立详解

- [[习题 - 池化、下采样、混叠与不变性边界]]
- [[解答 - 池化、下采样、混叠与不变性边界]]

## 参考来源

- [[S-2019-Zhang-AntiAliased-CNN]]
- [[S-2016-Goodfellow-Bengio-Courville-Deep-Learning]]
- [[S-2023-Zhang-Lipton-Li-Smola-D2L]]
