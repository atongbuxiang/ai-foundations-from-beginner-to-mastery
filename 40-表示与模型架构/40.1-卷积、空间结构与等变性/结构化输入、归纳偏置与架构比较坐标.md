---
type: concept
status: draft
area: [architecture, inductive-bias, structured-data]
aliases: [架构比较坐标, 结构化输入与归纳偏置]
node_id: ARCH-01
prerequisites: ["[[预测器、假设空间与学习算法]]", "[[线性层、批量张量与参数计数]]", "[[数据增强、不变性、等变性与任务充分性]]"]
related: ["[[离散卷积、互相关与边界约定]]", "[[序列因果性、隐藏状态与递推计算]]", "[[图数据、节点重标号与置换对称性]]", "[[内容寻址、Query、Key 与 Value]]"]
sources: ["[[S-2016-Goodfellow-Bengio-Courville-Deep-Learning]]", "[[S-2023-Zhang-Lipton-Li-Smola-D2L]]", "[[S-2016-Cohen-Welling-GCNN]]", "[[科学空间 - 第四章专题来源地图]]"]
exercises: ["[[习题 - 结构化输入、归纳偏置与架构比较坐标]]"]
solutions: ["[[解答 - 结构化输入、归纳偏置与架构比较坐标]]"]
figure: "[[00-知识库管理/_assets/figures/architecture/fig-architecture-comparison-contract-v1.svg]]"
created: 2026-08-24
updated: 2026-08-29
---

# 结构化输入、归纳偏置与架构比较坐标

> [!abstract] 本节主问题
> 架构不是一串层名，而是对输入结构、允许的信息交换和资源预算作出的假设。卷积假设局部模式可跨位置复用；RNN/SSM 假设历史可被有限状态递推压缩；GNN 尊重节点重标号；Attention 让交互边由当前内容决定。没有脱离数据分布、任务和硬件的“绝对最佳架构”。

## 课程位置与两遍学习路线

- **承接什么：** 你已经知道预测器属于某个假设空间，也见过 dense layer、数据增强和等变性；现在要把这些概念变成“为什么要选择某种架构”的可执行判断；
- **本页解决什么：** 在看到 CNN、RNN、GNN 或 Attention 的公式以前，先建立输入结构、群作用、信息路径、资源与证据五类对象；
- **后续为何需要：** ARCH-02—08 会把这套坐标应用到卷积；40.2—40.8 只替换交互图、状态或路由合同，不再重新发明比较标准。

**第一遍只建立选择顺序。** 遇到一个任务，先问输入的索引之间有什么关系，再问哪些变换应保留语义，最后才比较层名。第一遍读到贯穿算例后，应能解释“flatten 没有删除数值，却删除了模型可直接利用的邻接合同”。

**第二遍重建约束。** 把归纳偏置写成函数类或参数共享限制，逐项检查 equivariance/invariance，并为参数、算术、激活、IO、串行深度与通信分别记账。

### 问题链

1. 同一个数值数组为什么可以代表完全不同的学习问题？
2. 输入结构怎样变成模型允许的信息边与参数共享规则？
3. 等变与不变分别保留或删除什么信息？
4. 强结构先验为何可能提高样本效率，又为何可能产生不可消除的错配？
5. 为什么参数量、FLOPs 和真实延迟不能互相替代？
6. 面对陌生架构，怎样区分恒等式、定理、实验和作者解释？

> [!check] 第一遍停靠线
> 若你能对“平移不改变类别的短序列检测”写出 index set、邻域、希望的等变/不变关系，并说明 dense、conv 与 attention 的一步信息边有何不同，就可以先进入 ARCH-02。资源精算和证据分级留到第二遍。

## 符号与对象账本

| 对象 | 数学类型 | 在 AI 中的身份 | 不能偷换成 |
|---|---|---|---|
| $\mathcal I$ | 索引集合 | 像素位置、时间步、节点或 token 位置 | feature 数值本身 |
| $X=(x_i)_{i\in\mathcal I}$ | 带索引的特征族 | 一条结构化输入 | 无结构样本集合 |
| $\mathcal R$ | 邻接、顺序、坐标或群作用 | 模型可利用的结构合同 | 从数组形状自动推出的语义 |
| $T_g,S_g$ | 输入与输出空间上的作用 | 数据变换及表示的相应变换 | 数据增强样本 |
| $f$ | 从输入表示到输出表示的映射 | 一个具体架构实例 | 训练算法或数据分布 |
| $\mathcal H_{\mathcal A}$ | 架构诱导的函数类 | 架构归纳偏置 | “一定泛化更好”的保证 |
| $C_{\mathrm{param}},C_{\mathrm{MAC}},C_{\mathrm{IO}}$ | 不同资源量 | 参数、算术、搬运账 | 单一“模型大小” |

### 贯穿算例：局部对比检测卡 $\mathcal C_\square$

ARCH-01—04 反复使用同一张一维信号卡：

$$
x_\square=(2,-1,3,0,1),
\qquad
w_\square=(1,0,-1).
$$

任务不是记住这五个数，而是检测长度为 3 的局部左右对比。索引集合是有顺序的线 $\mathcal I=\{0,1,2,3,4\}$，邻域由相对 offset $(0,1,2)$ 给出。若把局部模式整体平移，我们希望中间响应随之平移；若最后只问“是否出现过这种模式”，再通过合适 readout 获得不变性。

在 valid cross-correlation 约定下，三个窗口的输出是

$$
y_\square=(-1,-1,2).
$$

ARCH-02 会逐项复算这三个数，ARCH-03 会把输入放到周期域上验证交换恒等式，ARCH-04 会把它提升为多通道张量并完成 shape/parameter/MAC 总账。

## 核心公式七问：$f(T_gx)=S_gf(x)$

| 问题 | 本式的回答 |
|---|---|
| 目的 | 精确表达“输入发生结构变换时，中间表示应怎样跟着变” |
| 对象 | $T_g$ 作用于输入空间，$S_g$ 作用于输出空间；两侧必须落在同一个输出空间 |
| 来路 | 先走“变换输入再计算”与“先计算再变换输出”两条路径，并要求它们相等 |
| 步骤 | 必须先声明群 $G$、两种作用及 $f$ 的定义域；只写“模型对平移鲁棒”不足以推出此式 |
| 读法 | 模型保留了变换关系，而不是把变换造成的位置信息立即删除 |
| 检查 | 取 $g=e$ 得恒等式；若 $S_g=I$ 则退化为 invariance；若两侧 shape 不同，声明本身无意义 |
| 去路 | ARCH-03 用共享卷积证明平移等变，40.3 用节点置换，40.6 用位置编码检查对称性的保留与破坏 |

## 一、学完应能做什么

1. 区分输入数值、索引结构、邻接关系和群作用；
2. 用假设空间/参数约束解释 inductive bias；
3. 区分 invariance 与 equivariance；
4. 为 dense、convolution、recurrence、message passing、attention 画信息交互图；
5. 分开参数量、算术量、激活内存、IO、串行深度和通信；
6. 解释强先验为什么既能提高样本效率，也会造成错配；
7. 把恒等式、定理、实验、解释性假说和开放问题分层；
8. 对陌生架构写出一页最小审计卡。

## 二、先看一个被 flatten 破坏的问题

一张 $32\times32\times3$ 图像有 3072 个数。把它 flatten 成 $x\in\mathbb R^{3072}$ 后，dense layer 当然仍能接收全部像素，但向量坐标本身没有告诉模型：

- 第 17 个和第 18 个坐标在图上是否相邻；
- 某个边缘从左移到右后是否应使用同一 detector；
- 横向移动、颜色通道置换和任意坐标置换是否具有相同语义。

“信息还在数组里”不等于“模型容易利用结构”。Dense network 可以在足够参数和数据下学习许多结构，但它没有被迫以局部共享方式学习。

## 三、结构化输入究竟多了什么

把数据写成 $(\mathcal I,X,\mathcal R)$：

- $\mathcal I$ 是 index set，如像素坐标、时间位置或图节点；
- $X=(x_i)_{i\in\mathcal I}$ 是每个位置上的 feature；
- $\mathcal R$ 是额外结构，如网格邻域、先后关系、图边或允许的变换群。

同一个数值矩阵若赋予不同 $\mathcal R$，就代表不同问题。时间序列的第 $t$ 行和无序样本表的第 $t$ 行都可存在数组中，但前者的顺序通常不能任意打乱，后者的样本顺序通常应该不影响训练目标。

## 四、Inductive Bias 的形式化含义

学习算法从数据中选择函数 $f$。归纳偏置可以通过多种方式进入：

1. **假设空间限制**：只允许局部共享线性算子，而非任意 dense matrix；
2. **参数共享**：多个位置调用同一 kernel；
3. **计算图限制**：节点只从邻居接收消息；
4. **正则化/先验**：偏好平滑、小范数或某种对称性；
5. **优化偏置**：相同函数类中，训练算法更容易找到某些解。

因此“CNN 的 bias 是局部性”只说了一部分。还要说明局部连接、跨位置共享、层叠扩大感受野、下采样以及最终读出分别施加了什么限制。

## 五、不变与等变

设变换群 $G$ 作用于输入，记 $T_gx$。若输出也有作用 $S_g$，则

$$
\boxed{f(T_gx)=S_g f(x)}
$$

称 $f$ 对该变换 **equivariant**。若输出不随变换变化，$S_g=I$，则

$$
\boxed{f(T_gx)=f(x)}
$$

称为 **invariant**。

平移一张图，feature map 也相应平移，是等变；平移后类别不变，是不变。中间表示通常需要保留“东西移到了哪里”，过早要求不变可能丢失定位信息。

## 六、四类信息混合机制

令输入有 $n$ 个位置，每个位置宽度 $d$。

| 机制 | 一步交互图 | 共享方式 | 典型优点 | 典型风险 |
|---|---|---|---|---|
| Dense | 所有 feature 与所有输出相连 | 通常不跨位置显式共享 | 通用、简单 | 参数大，结构先验弱 |
| Convolution | 固定局部邻域 | kernel 跨位置共享 | 局部、平移等变、并行 | 远程交互需深度，采样会混叠 |
| RNN/SSM | 状态沿时间链传播 | transition 跨时间共享 | 因果流式、固定状态 | 串行/记忆瓶颈、梯度问题 |
| Attention | 当前内容决定加权边 | Q/K/V projection 共享 | 一步全局、关系可变 | $n^2$ 矩阵、位置需另注入 |

GNN 的 message passing 可看成“邻接由图结构指定、参数跨节点/边共享”的另一类结构化混合。

## 七、为什么强 Bias 可能更省数据

如果真实规律确实平移共享，那么 convolution 不必分别学习“左上角竖边”和“右下角竖边”。共享核把多个位置的观察合并为对同一参数的证据，降低自由度并提高统计效率。

但这是条件句。若绝对位置本身决定标签，例如医学图像中某器官只可能出现在特定区域，完全平移不变的读出可能过强；如果传感器不同位置有不同校准误差，强制共享也可能错配。

> [!warning] Bias–mismatch 原则
> 好的归纳偏置是“与任务生成机制近似匹配的限制”，不是“越强越好”。错配会带来 approximation bias，即使训练数据无限也可能无法表达真实函数。

## 八、资源必须分账

至少记录六个量：

1. **parameters**：模型静态参数；
2. **arithmetic**：MACs/FLOPs；
3. **activation memory**：训练中为反向保存的中间量；
4. **state/cache**：流式或自回归推理保留的状态；
5. **memory IO**：数据在层级存储间搬运量；
6. **sequential depth / communication**：不可并行步骤和设备间数据交换。

参数更少不必然更快；FLOPs 更少也不必然低延迟。小而碎的算子、低算术强度、随机稀疏访问或 all-to-all 通信都可能主导时间。

## 九、一个最小架构审计例子

任务：长度 $n=1024$ 的传感器序列，需要实时检测局部尖峰，同时偶尔依赖远处状态。

- 纯 dense：输入长度固定，参数可能随 $n$ 增长，不适合流式；
- 小卷积：尖峰局部模式合适、可并行，但有限层难覆盖远距；
- RNN/SSM：流式状态自然，能压缩历史，但必须验证状态容量和稳定性；
- full attention：远程匹配直接，却需缓存历史且单步读全部 key/value；
- hybrid：短卷积负责局部、状态/attention 负责远程，代价是接口和归因更复杂。

这不是立即选出答案，而是把比较问题变成可测合同。

## 十、图：从输入结构到比较坐标

先看图回答：同一组输入位置经过不同架构后，究竟改变的是参数共享、交互边，还是资源账本？

![[00-知识库管理/_assets/figures/architecture/fig-architecture-comparison-contract-v1.svg|900]]

> [!figure] 图 40.1-01　结构、信息混合图与架构比较坐标
> 左栏区分数值数组与结构；中栏用不同连接图表示 dense、conv、RNN 和 attention；右栏列出对称性、交互范围、参数、算术、内存/IO 与证据六本账。来源：依据 Goodfellow–Bengio–Courville、D2L 与群等变网络文献独立绘制；由 [[00-知识库管理/_labs/code/plot_architecture_convolution_foundations_v1.py]] 确定性生成。

**怎样读图**：先问 index 是否有几何/顺序/邻接，再读一步信息边，最后逐项记成本与证据。不要从“连接更全”直接推出“模型更好”。

**图没有证明什么**：它没有证明某一架构在任何数据上更准确，也没有把实际 kernel latency、优化难度或规模化规律压缩成一个图标。

## 十一、科学空间在本章的角色

[[科学空间 - 第四章专题来源地图]]特别适合展示“从一个代数问题一路追到现代结构”的研究方式，例如从置换对称到位置编码，从核特征到线性 Attention，从正交投影到 SSM，从负载统计到 MoE 路由。

它对 40.1 的直接覆盖较弱，所以 CNN 的正式骨架不能从博客文章数量反推；本卷主要依赖教材与 CNN/等变网络原论文。后续引用科学空间时，必须把可复算恒等式 `I`、定理 `T`、实验 `E`、解释 `H` 和开放问题 `O` 分开。

## 十二、常见误区

1. “flatten 后数值没丢，所以结构也没丢”；
2. “有平移等变就自动有平移不变分类”；
3. “Attention 没有 inductive bias”；它仍有共享投影、softmax、head 分解等结构；
4. “参数少就速度快”；忽略 IO、并行和 kernel；
5. “全局交互一定优于局部交互”；忽略统计偏置和噪声；
6. 用单一 benchmark 给架构下永久排名；
7. 把作者解释或小规模消融写成普遍机制定理。

## 十三、架构审计模板

遇到陌生模型，依次填写：

| 项目 | 必答问题 |
|---|---|
| 对象 | 输入位置、feature、边/顺序/坐标是什么？ |
| 对称性 | 希望对哪些变换等变/不变？实际算子满足吗？ |
| 信息路径 | 一层、$L$ 层和推理状态各能看到什么？ |
| 形状 | 每个中间张量和 contracted axis 是什么？ |
| 资源 | 参数、FLOPs、激活、cache、IO、通信分别多少？ |
| 数值 | normalization、mask、padding、精度与稳定性如何？ |
| 证据 | 哪些是 I/T/E/H/O？比较是否公平？ |
| 失败 | 哪个假设最可能与任务错配？ |

## 十四、回顾与掌握标准

> [!summary]
> - 架构把结构假设编码成参数共享和计算图；
> - equivariance 保持变换关系，invariance 删除变换影响；
> - 局部、状态、邻接和内容寻址是不同信息路径；
> - 资源与证据都必须分账；
> - bias 匹配带来效率，错配带来不可消除的系统偏差。

能识别对象（A）、为小任务选比较坐标（B）、证明简单等变/不变关系（C）、构造 bias mismatch 反例（D），并对陌生架构完成八项审计（E）。

## 十五、练习与独立详解

- [[习题 - 结构化输入、归纳偏置与架构比较坐标]]
- [[解答 - 结构化输入、归纳偏置与架构比较坐标]]

## 参考来源

- [[S-2016-Goodfellow-Bengio-Courville-Deep-Learning]]
- [[S-2023-Zhang-Lipton-Li-Smola-D2L]]
- [[S-2016-Cohen-Welling-GCNN]]
- [[科学空间 - 第四章专题来源地图]]
