---
type: experiment
status: draft
area: [math/linear-algebra, math/tensors, ai/representations]
question: "坐标病态、非正规谱与非线性 rank 边界如何用同一套 space—map—norm—evidence 合同区分？"
hypothesis: "基趋于共线时同一向量的坐标与条件数按 1/epsilon 增长；相同 eigenvalues 不控制 Jordan 系统的 transient；rank-2 attention score 经 row-softmax 后可以 numerical full rank。"
code: "[[plot_linear_algebra_cumulative_gate.py]]"
data: "完全合成：二维病态基、三维子空间、2x2 Jordan 系统、对角 singular spectrum、8-token attention score"
seed: 20260820
related: ["[[阶段测验 - 线性代数（10.2）]]", "[[阶段测验解答 - 线性代数（10.2）]]", "[[线性代数 MOC]]", "[[线性代数完整学习路线与掌握标准]]"]
created: 2026-08-20
updated: 2026-08-23
---

# 实验 - 线性代数累计复现门

> [!abstract] 实验定位
> 这是`LA-CUM-01`的计算复现门。三条轨道分别验收“坐标—子空间—商”“谱—Jordan—SVD”和“Kronecker—attention—非线性边界”。脚本的作用是核对推导、暴露浮点阈值和建立可复现实验；它不能替代对exact rank、spectral theorem或Eckart–Young定理的证明。

## 一、预注册问题与通过标准

### A. 同一向量为何会有巨大坐标？

对

$$
B_\varepsilon=
\begin{bmatrix}
1&1\\
0&\varepsilon
\end{bmatrix},
\qquad
v=\begin{bmatrix}0\\1\end{bmatrix},
$$

把$B_\varepsilon$的两列视为$\mathbb R^2$的一组basis。检验：

1. abstract vector $v$保持不变；
2. coordinate column $[v]_{B_\varepsilon}$是否随$\varepsilon\downarrow0$增大；
3. $\kappa_2(B_\varepsilon)$是否以相同量级增长；
4. 三维例子中，改变quotient representative是否保持线性映射输出不变；
5. 由column space构造的$P$是否满足$P^2=P=P^T$。

> [!hypothesis]
> $[v]_{B_\varepsilon}=(-1/\varepsilon,1/\varepsilon)^T$，故坐标norm为$\sqrt2/\varepsilon$；$\kappa_2(B_\varepsilon)\sim2/\varepsilon$。这说明“大坐标”可来自basis geometry，而不代表abstract vector本身变大。

### B. 相同eigenvalues为何有不同动力学？

比较

$$
D=\rho I,
\qquad
J_\rho=\begin{bmatrix}\rho&1\\0&\rho\end{bmatrix},
\qquad \rho=0.9.
$$

二者eigenvalues都为$0.9,0.9$。计算$k=0,\ldots,60$的$\|D^k\|_2$与$\|J_\rho^k\|_2$，并记录transient peak。另对singular values

$$
(5,2,0.5,0.1)
$$

核对truncated SVD的spectral/Frobenius tail error。

> [!hypothesis]
> $D^k$单调衰减；$J_\rho^k$因$k\rho^{k-1}$先增长后衰减。最佳rank-$r$近似误差分别等于$\sigma_{r+1}$和$\sqrt{\sum_{i>r}\sigma_i^2}$，但该parameter/operator误差不自动等于task error。

### C. Attention的rank上界能否穿过softmax？

取$T=8,d=2$，用二维unit vectors构造$Q,K\in\mathbb R^{8\times2}$，令

$$
S=2QK^T,
\qquad
P=\operatorname{softmax}_{row}(S).
$$

结构上$\operatorname{rank}S\le2$。比较$S$与$P$的singular spectrum；再用第8题矩阵核对

$$
\operatorname{vec}(AXB)=(B^T\otimes A)\operatorname{vec}(X).
$$

> [!hypothesis]
> 当前构造中$P$为numerical full rank 8。这只需作为“softmax不保持rank”的一个existence counterexample；不主张所有softmax输出都full rank。

### 通过条件

- 不看代码先写出A、B的exact formula与C的shape/index合同；
- full run生成XML合法的SVG，hash与基准一致，或对差异给出环境/代码解释；
- 能区分exact rank与numerical rank threshold；
- 随机指定一条轨道完成两项手工复核和一次**运行前**参数预测；
- 能写出每条轨道至少一项“实验不能推出”的结论。

## 二、对象—定理—计算—边界

| 轨道 | exact对象 | 定理或结构保证 | 数值检查 | 不可推出 |
|---|---|---|---|---|
| A | basis map $B_\varepsilon:\mathbb R^2_{coord}\to\mathbb R^2$；quotient map；projector | 可逆basis给唯一coordinates；第一同构；orthogonal projector | condition、reconstruction、$P^2-P$、代表不变性 | 坐标大不等于abstract vector大；小residual不保证well-conditioned |
| B | endomorphisms $D,J_\rho$；singular spectrum | Jordan power公式；Eckart–Young–Mirsky | power norms、peak、SVD tail | eigenvalues相同不保证norm/transient相同；低operator error不保证task等价 |
| C | bilinear score map、row-softmax、Kronecker operator | $\operatorname{rank}(QK^T)\le d$；vec identity | 两组singular values、numerical rank、vec residual | 线性rank上界不能穿过nonlinearity；显式Kronecker不是推荐实现 |

## 三、推导

### 3.1 病态basis与坐标增长

由

$$
B_\varepsilon
\begin{bmatrix}c_1\\c_2\end{bmatrix}
=\begin{bmatrix}c_1+c_2\\\varepsilon c_2\end{bmatrix}
=\begin{bmatrix}0\\1\end{bmatrix}
$$

立刻得到

$$
c_2=\frac1\varepsilon,
\qquad
c_1=-\frac1\varepsilon,
\qquad
\|[v]_{B_\varepsilon}\|_2=\frac{\sqrt2}{\varepsilon}.
$$

另一方面

$$
B_\varepsilon^TB_\varepsilon=
\begin{bmatrix}
1&1\\
1&1+\varepsilon^2
\end{bmatrix}.
$$

其trace为$2+\varepsilon^2$、determinant为$\varepsilon^2$，所以两个eigenvalues为

$$
\lambda_\pm
=\frac{2+\varepsilon^2\pm\sqrt{4+\varepsilon^4}}2,
$$

从而

$$
\kappa_2(B_\varepsilon)
=\sqrt{\frac{\lambda_+}{\lambda_-}}
\sim\frac2\varepsilon.
$$

当$\varepsilon\to0$，两根basis vectors趋于共线；用两个巨大的、几乎抵消的coefficients表达一个unit vector，正是coordinate instability的几何来源。

三维核—像检查使用

$$
A=\begin{bmatrix}1&0&1\\0&1&1\\1&1&2\end{bmatrix},
\qquad
n=(-1,-1,1)^T\in\mathcal N(A).
$$

于是exactly有$A(x+tn)=Ax$。若

$$
C=\begin{bmatrix}1&0\\0&1\\1&1\end{bmatrix},
$$

则到$\mathcal R(C)=\mathcal R(A)$的orthogonal projector为

$$
P=C(C^TC)^{-1}C^T
=\frac13\begin{bmatrix}
2&-1&1\\
-1&2&1\\
1&1&2
\end{bmatrix}.
$$

exact arithmetic中$P^2=P=P^T$；脚本报告的是浮点residual，而不是重新证明这些等式。

### 3.2 Jordan transient与SVD tail

令$N=\begin{bmatrix}0&1\\0&0\end{bmatrix}$，则$N^2=0$且$J_\rho=\rho I+N$。因此

$$
J_\rho^k
=\rho^k I+k\rho^{k-1}N
=\begin{bmatrix}
\rho^k&k\rho^{k-1}\\
0&\rho^k
\end{bmatrix}.
$$

虽然spectral radius为$0.9<1$，off-diagonal项在早期随$k$增长，导致operator norm先上升。对任意$2\times2$上三角矩阵$M=\begin{bmatrix}a&b\\0&a\end{bmatrix}$，脚本由$M^TM$的最大eigenvalue计算

$$
\|M\|_2^2
=\frac{2a^2+b^2+\sqrt{(2a^2+b^2)^2-4a^4}}2.
$$

这不是“eigenvalue分析错误”，而是eigenvalues本来就没有对一般non-normal矩阵承诺短时operator norm。

对于singular values$\sigma_1\ge\cdots\ge\sigma_4$，Eckart–Young–Mirsky给出

$$
\min_{\operatorname{rank}(X)\le r}\|M-X\|_2=\sigma_{r+1},
\qquad
\min_{\operatorname{rank}(X)\le r}\|M-X\|_F
=\sqrt{\sum_{i>r}\sigma_i^2}.
$$

这两项都是matrix approximation合同；经过输入分布、nonlinearity、residual与多层composition后，需要另建function/task error账本。

### 3.3 Score rank、softmax与numerical rank

因$Q\in\mathbb R^{8\times2}$、$K^T\in\mathbb R^{2\times8}$，

$$
\operatorname{rank}(QK^T)
\le\min(\operatorname{rank}Q,\operatorname{rank}K)
\le2.
$$

但

$$
P_{ij}=\frac{e^{S_{ij}}}{\sum_{\ell=1}^{8}e^{S_{i\ell}}}
$$

是nonlinear transformation。逐元素指数既不等于左乘或右乘某个固定矩阵，也不是rank-preserving map。本实验的$P$最小singular value约为$4.21\times10^{-5}$，在relative threshold $10^{-6}\sigma_1$下判为rank 8。

脚本对$S$算出的第三、第四singular values约为$10^{-8}$，这是小型Jacobi eigensolver在零eigenvalues附近的浮点残留。**Exact rank 2来自factorization $S=2QK^T$；不是由把阈值调到2才得到。**这正是structural rank与numerical rank必须并列记录的理由。

vec恒等式则是纯线性结构。column-major约定下，先按block定义展开$B^T\otimes A$，再核对两侧shape均为$mq$；实践实现通常用reshape与两次matmul，而不物化巨大Kronecker matrix。

## 四、代码、图形与环境

- 代码：[plot_linear_algebra_cumulative_gate.py](</Users/tong/Nodes/basic/00-知识库管理/_labs/code/plot_linear_algebra_cumulative_gate.py>)；
- 图形：[plot-linear-algebra-cumulative-gate-v2.svg](</Users/tong/Nodes/basic/00-知识库管理/_assets/figures/linear-algebra/plot-linear-algebra-cumulative-gate-v2.svg>)；
- full-run SVG SHA-256：`b7e68c4a89ef2c075aff5918dd1c3a9cda385d7c6558f595c83c6ffc92fe9e39`；
- 环境：Python 3.9.6，仅使用标准库；
- SVG不含时间戳，full run应确定性复现；
- `seed=20260820`为后续随机干预保留，本基准主体是确定性构造。

## 五、结果

先看图回答：同一抽象向量、同一特征值集合和一个 rank-2 score，为什么分别能够产生大坐标、暂态放大和 full-rank attention？

![[00-知识库管理/_assets/figures/linear-algebra/plot-linear-algebra-cumulative-gate-v2.svg|880]]

> [!figure] 实验图｜坐标病态、Jordan 暂态与 softmax 后秩增长
> A 固定抽象向量并让基趋于共线，比较坐标范数与基条件数；B 在相同 eigenvalues 下比较 normal 与 Jordan powers，并附 SVD 尾误差；C 展示结构上 rank 至多 2 的 score 经 row-softmax 后数值秩为 8。生成脚本：[[plot_linear_algebra_cumulative_gate.py]]；确定性构造，并对坐标放大、Jordan 暂态、vec 恒等式和秩增长设断言。

**怎样读图。** A 不把“大坐标”误作“大向量”，而要连同 basis map 一起读；B 不能只看谱半径，要观察有限 $k$ 的 operator norm；C 先用 factorization 认证 score 的 exact rank，再用固定相对阈值读取 softmax 后 singular values。

**适用边界（图没有证明什么）。** 二维病态基、$2\times2$ Jordan 块与 8-token attention 都是教学构造；图不证明一般模型的 attention 总为满秩，也不从单一数值阈值推出结构秩，更不替代非正规谱理论或生产矩阵算法分析。

> [!question] 本实验的判别问题
> 如何区分向量与坐标、特征值与算子幂、exact rank 与 numerical rank，使线性结构经过非线性 AI 运算后仍保持正确的对象层级？

图注：A展示abstract vector固定时coordinates与basis condition同步增长，并附商空间/projector不变量；B显示相同eigenvalues下normal与non-normal powers的差异，并给truncated-SVD tail；C显示结构上rank至多2的score经row-softmax后在预注册阈值下rank为8。

### 5.1 坐标、条件数与子空间invariants

| $\varepsilon$ | $\kappa_2(B_\varepsilon)$ | $\|[v]_{B_\varepsilon}\|_2$ | reconstruction residual |
|---:|---:|---:|---:|
| 1 | 2.618034 | 1.414214 | 0 |
| 0.3 | 6.820040 | 4.714045 | 0 |
| 0.1 | 20.050125 | 14.142136 | 0 |
| 0.03 | 66.681670 | 47.140452 | 0 |
| 0.01 | 200.005000 | 141.421356 | 0 |
| 0.003 | 666.668170 | 471.404521 | 0 |

其余residual：$\|An\|_2=0$；$\|A(x+3.7n)-Ax\|_2=9.16\times10^{-16}$；$\|P^2-P\|_F=1.57\times10^{-16}$；$\|P^T-P\|_F=0$。后两个接近machine precision支持“实现与exact derivation一致”，但不应被表述为浮点数中严格等于零。

### 5.2 Jordan powers与low-rank tail

| $k$ | $\|D^k\|_2$ | $\|J_\rho^k\|_2$ |
|---:|---:|---:|
| 0 | 1.000000 | 1.000000 |
| 1 | 0.900000 | 1.529563 |
| 5 | 0.590490 | 3.383551 |
| **9** | 0.387420 | **3.912567** |
| 10 | 0.348678 | 3.905336 |
| 20 | 0.121577 | 2.707163 |
| 40 | 0.014781 | 0.657261 |
| 60 | 0.001797 | 0.119828 |

$J_\rho$最终仍趋于零，但在$k=9$达到约3.91的峰值。只报告spectral radius会漏掉这个训练/推理时间尺度上的行为。

| retained rank $r$ | spectral error | Frobenius error |
|---:|---:|---:|
| 1 | 2.0 | 2.063977 |
| 2 | 0.5 | 0.509902 |
| 3 | 0.1 | 0.1 |
| 4 | 0 | 0 |

### 5.3 Softmax rank与vec identity

| 对象 | singular values（降序） | numerical rank |
|---|---|---:|
| $S=2QK^T$ | 8.9441, 7.0449, $5.55\times10^{-8}$, $4.63\times10^{-8}$, 0, 0, 0, 0 | 2 |
| $P=\operatorname{softmax}_{row}(S)$ | 1.0225, 0.6025, 0.2351, 0.07151, 0.01777, 0.003460, 0.0005124, 0.00004208 | 8 |

relative threshold为$10^{-6}$。vec轨道两侧都得到

$$
(14,10,6,4)^T,
$$

residual为0。

## 六、复现命令

在仓库根目录运行：

```bash
python3 "00-知识库管理/_labs/code/plot_linear_algebra_cumulative_gate.py"
xmllint --noout "00-知识库管理/_assets/figures/linear-algebra/plot-linear-algebra-cumulative-gate-v2.svg"
shasum -a 256 "00-知识库管理/_assets/figures/linear-algebra/plot-linear-algebra-cumulative-gate-v2.svg"
```

如需临时输出，必须使用`--output`，不要覆盖基准图：

```bash
python3 "00-知识库管理/_labs/code/plot_linear_algebra_cumulative_gate.py" \
  --output "/tmp/linear-algebra-gate-intervention.svg"
```

## 七、随机指定轨道的手工复核

### A轨道：至少两项

1. 手推$[v]_{B_\varepsilon}$与$\kappa_2(B_\varepsilon)$；
2. 手算$P=C(C^TC)^{-1}C^T$并核对$Pn=0$；
3. 将$v$换为$(1,0)^T$前，预测其coordinates是否同样爆炸，再运行；
4. 将basis换为orthogonal but differently scaled的矩阵，区分angle与scale造成的conditioning。

### B轨道：至少两项

1. 从binomial theorem推导$J_\rho^k$；
2. 用$M^TM$手推2-by-2 operator norm公式；
3. 把$\rho$改为$0.8$或$0.99$前，预测peak高度与位置变化；
4. 自选一组singular values，在运行前写出每个rank的两种tail error。

### C轨道：至少两项

1. 用shape与rank inequality证明$\operatorname{rank}(QK^T)\le2$；
2. 手算第8题的$B^T\otimes A$及column-vec；
3. 在运行前把score scale从2改为0、0.5或4，预测softmax spectrum变化；
4. 改变numerical-rank threshold并解释哪些改变是reporting convention，哪些是exact structure。

## 八、不能越界的结论

1. 一个病态basis例子不证明所有大activation/parameter coordinates都由换基造成；
2. $\rho<1$保证该固定finite Jordan block的powers最终衰减，但不保证所有有限时域都contractive，也不自动覆盖time-varying/nonlinear systems；
3. truncated SVD是指定unitarily invariant norm下的最佳矩阵近似，不保证最佳下游loss；
4. 本例证明softmax**可以**提高rank，不证明它对任意score都产生full rank；
5. 小vec residual只核对当前index convention与实现，不证明显式Kronecker物化高效；
6. numerical rank依赖scale、noise与threshold，必须与exact factorization分开声明。

## 九、通过记录

| 项目 | 记录 |
|---|---|
| 随机指定轨道 |  |
| 日期与用时 |  |
| Python/平台 |  |
| full命令 |  |
| 新SVG SHA-256 |  |
| hash差异解释 |  |
| 手工复核1 |  |
| 手工复核2 |  |
| 参数干预前预测 |  |
| 运行结果与偏差解释 |  |
| exact / numerical / empirical边界 |  |
| 48小时复测 |  |
| 14天迁移 |  |
| 结论 | `passed / needs-remediation / not-attempted` |

当前默认状态：`not-attempted`。
