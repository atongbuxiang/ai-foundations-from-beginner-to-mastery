---
type: derivation
status: draft
area: [neural-networks/residual-stability, resnet, optimization]
aliases: [Residual Learning and Identity Shortcuts, ResNet Degradation Problem]
node_id: NN-41
prerequisites: ["[[多层感知机与逐层前向计算]]", "[[标量链式法则与反向传播递推]]", "[[Pre-Norm、Post-Norm 与归一化放置]]"]
related: ["[[残差块 Jacobian 与梯度直通]]", "[[ResNet 的 ODE 与离散动力系统视角]]", "[[Pre-Activation、Pre-Norm 与 Post-Norm 残差]]", "[[Highway、Dense Connection 与 Skip 结构比较]]"]
sources: ["[[S-2016-He-ResNet]]", "[[S-2016-He-Identity-Mappings]]", "[[S-2022-Su-8994-Why-Residual]]"]
exercises: ["[[习题 - 残差学习、恒等捷径与退化问题]]"]
solutions: ["[[解答 - 残差学习、恒等捷径与退化问题]]"]
figure: "[[00-知识库管理/_assets/figures/neural-networks/fig-residual-identity-degradation-v2.svg]]"
created: 2026-08-23
updated: 2026-08-29
---

# 残差学习、恒等捷径与退化问题

> [!abstract] 本章主问题
> 残差连接不是“在输出上顺手加回输入”，而是把每个 block 的默认任务从重新构造整个映射，改成在一条显式基准路径上学习增量。它提供可实现的 identity 基线和直接的信息通道，但能表示 identity、梯度里出现 $I$、深网容易优化与最终性能更好是四个强度不同的命题。

## 课程位置与两遍学习路线

- **承接什么：** 30.5 已说明 normalization placement 会决定 identity rail 是否被额外 Jacobian 过滤；
- **本页解决什么：** 在暂不加入 normalization 的最小 residual chain 中，区分目标映射、residual parameterization、函数类嵌套、优化可达性与经验性能；
- **后续为何需要：** NN-42—44 将对同一 chain 求 Jacobian、解释为 Euler 网格并建立深度稳定界，必须先把前向对象和证据等级固定。

**第一遍只跟踪状态。** 从 $x_{\ell+1}=x_\ell+F_\ell(x_\ell)$ 逐层写出 state、branch increment 和最终 residual sum；看清 $F=0$ 为什么给出 identity baseline。

**第二遍再讨论“为什么有效”。** 检查 shape/projection、zero-branch 实现、normalization、有限精度和优化器，严格分开“能表示”“容易找到”“泛化更好”三种命题。

### 问题链

1. plain block 学 $H$ 与 residual block 学 $F=H-I$ 在函数上等价，参数坐标为何不同？
2. 新增一个 residual block 何时能精确保持旧网络函数？
3. shortcut 不是 identity、branch 不能置零或后接 activation 时，函数类嵌套为何可能失效？
4. 多层 residual stream 的“输入加增量和”是精确恒等式还是近似？
5. ResNet 的 degradation 实验支持哪个层级的结论，又不支持哪些更强叙事？

> [!check] 第一遍停靠线
> 若你能在 $\mathcal R_\square$ 中从 $x_0=(1,1)$ 算到 $x_4=(1.21550625,1/16)$，并把 $x_4-x_0$ 还原为四次 branch increments 的精确和，就已掌握本页主干。

## 符号与对象账本

| 对象 | 定义 | 课程身份 | 不自动保证 |
|---|---|---|---|
| $H_\ell$ | plain block 的完整目标映射 | 直接参数化对象 | 易优化 |
| $F_\ell=H_\ell-I$ | identity 坐标下的 residual target | 增量参数化对象 | 值/Jacobian 一定小 |
| $x_{\ell+1}=x_\ell+F_\ell(x_\ell)$ | identity residual update | 状态递推 | 总 Jacobian 接近 $I$ |
| $P_\ell$ | shape-changing shortcut | projection baseline | 可逆或无损 |
| $\theta_\ell^0$ | 使 branch output 为 0 的参数点 | identity realization witness | 所有 branch parameters 都必须为 0 |
| degradation | 更深 plain net 训练误差反而更高 | 优化/参数化证据 | 普通 overfitting |

### 贯穿算例 $\mathcal R_\square$：同一残差链的两种方向

取

$$
A=
\begin{bmatrix}
1/5&0\\
0&-2
\end{bmatrix},
\qquad
T=1,
\qquad
N=4,
\qquad
h=\frac14,
$$

并令每层 branch 为

$$
F(x)=hAx=Bx,
\qquad
B=
\begin{bmatrix}
1/20&0\\
0&-1/2
\end{bmatrix}.
$$

一个 residual block 的完整映射矩阵是

$$
M=I+B
=\begin{bmatrix}
21/20&0\\
0&1/2
\end{bmatrix}.
$$

从 $x_0=(1,1)^{\mathsf T}$ 出发：

$$
\begin{aligned}
x_1&=\left(\frac{21}{20},\frac12\right)^{\mathsf T},\\
x_2&=\left(\frac{441}{400},\frac14\right)^{\mathsf T},\\
x_3&=\left(\frac{9261}{8000},\frac18\right)^{\mathsf T},\\
x_4&=\left(\frac{194481}{160000},\frac1{16}\right)^{\mathsf T}
=(1.21550625,0.0625)^{\mathsf T}.
\end{aligned}
$$

逐层展开不是把所有 branch 都错误地写成 $F(x_0)$，而是

$$
\boxed{
x_4=x_0+Bx_0+Bx_1+Bx_2+Bx_3
}.
$$

四个 increments 分别是

$$
\left(\frac1{20},-\frac12\right),
\left(\frac{21}{400},-\frac14\right),
\left(\frac{441}{8000},-\frac18\right),
\left(\frac{9261}{160000},-\frac1{16}\right).
$$

它们的和为

$$
x_4-x_0
=\left(\frac{34481}{160000},-\frac{15}{16}\right).
$$

第一坐标的四层目标只比 identity 大约 $21.55\%$，很自然地写成小正增量；第二坐标却要删除 $93.75\%$，并不“接近 identity”。残差参数化提供共同 baseline，但目标是否局部接近它仍是按方向、按数据域判断的条件。

## 核心公式七问：$x_{\ell+1}=x_\ell+F_\ell(x_\ell)$

| 问题 | 本式的回答 |
|---|---|
| 目的 | 在显式 baseline 上学习状态增量，而非每层重建完整映射 |
| 对象 | shape 一致的 hidden state 与同形 branch output |
| 来路 | 把目标 $H_\ell$ 精确重参数化为 $I+F_\ell$ |
| 步骤 | 计算当前 branch→与 shortcut 相加→把新 state 送入下一层 |
| 读法 | 多层输出是 $x_0+\sum_\ell F_\ell(x_\ell)$；每项求值点随深度变化 |
| 检查 | 令 branch 为 0 应恢复 identity；shape change 时必须显式检查 $P_\ell$ |
| 去路 | $I+J_F$、Euler step、residual scaling、skip taxonomy 与深层证据地图 |

### AI / 系统对应

CNN 的 stride/channel change、Transformer 的 attention/MLP 双子层、dropout、norm placement 与 low-precision addition 都会修改最小公式。可靠实现应在 step 0 记录 shortcut 类型、branch-output ratio $\|F(x)\|/\|x\|$、是否存在可实现的 zero branch，以及新增 blocks 能否数值上复现旧模型；这些观测比“用了 ResNet”更接近可证伪合同。

## 一、学习目标

读完本节，你应能：

1. 区分 approximation、optimization、generalization 三类“深度问题”；
2. 用函数与张量 shape 写出 identity/projection shortcut；
3. 从 $H(x)$ 推出 residual target $F(x)=H(x)-x$；
4. 说明 degradation problem 为什么不是普通 overfitting；
5. 给出“加层保持原函数”的精确条件及反例；
6. 展开多层 identity residual stream；
7. 比较 plain linear product 与 near-identity product；
8. 审计 zero branch、stride、channel change、normalization 和有限精度边界。

## 二、先区分三个问题

设训练集为 $S$，深度为 $L$ 的模型类为 $\mathcal H_L$。

### 2.1 表示问题

是否存在 $h\in\mathcal H_L$ 使目标风险很小？这是函数类问题。

### 2.2 优化问题

给定参数化、初始化与算法，是否能在有限预算内找到好参数？这是 trajectory 问题。

### 2.3 泛化问题

训练误差低后，测试风险是否低？这是数据分布与选择问题。

“网络更深却训练误差更高”首先指向优化或参数化困难，而不是“模型容量过大导致 overfitting”。overfitting 的典型现象是训练误差继续下降而测试误差变差。

## 三、plain block 与 residual block 的对象合同

对一个保持 shape 的 hidden state

$$
x_\ell\in\mathbb R^{B\times T\times D},
$$

plain block 写成

$$
x_{\ell+1}=H_\ell(x_\ell;\theta_\ell),
$$

identity residual block 写成

$$
\boxed{
x_{\ell+1}=x_\ell+F_\ell(x_\ell;\theta_\ell)
}.
$$

两个加数必须具有相同 shape、dtype、device 与布局。若空间尺寸、token 数或 channel 变化，则一般形式是

$$
x_{\ell+1}=P_\ell x_\ell+F_\ell(x_\ell;\theta_\ell),
$$

其中 $P_\ell$ 可能是 projection、stride、pooling 或 padding。此时 shortcut 不再是恒等映射。

## 四、为什么叫“学习残差”

假设 block 想实现目标映射 $H(x)$。定义

$$
F^\star(x)=H(x)-x.
$$

则

$$
H(x)=x+F^\star(x).
$$

这是精确重参数化，不代表 $F^\star$ 一定更简单。但若任务在某一表示层上接近 identity，例如只需小幅修正特征，则

$$
\|H(x)-x\|\ll\|H(x)\|
$$

使“零附近的 residual”成为自然起点。

> [!warning] 不要偷换“接近”
> 接近必须声明 norm、输入集合和尺度。平均样本上 $\|F(x)\|$ 小，不代表全局 Lipschitz 小；函数值小也不代表 Jacobian 小。

## 五、identity baseline 改变了什么

若 $F_\ell\equiv0$，则

$$
x_{\ell+1}=x_\ell.
$$

所以新增 block 可以在函数层面保持不变。这提供一个显式基准：新层不必一开始重建 $x$，只需决定偏离 identity 的方式。

plain block 若想实现 identity，必须让整个 $H_\ell$ 近似 $x$。对带非线性、normalization、bottleneck 或 shape change 的 block，这可能不是“把某个矩阵设为 $I$”那么简单。

## 六、加深后函数类是否嵌套

常见口号是

$$
\mathcal H_L\subseteq\mathcal H_{L+1},
$$

因为第 $L+1$ 个 residual branch 可设为零。这个包含关系需要：

1. 新 block 输入输出 shape 相同；
2. shortcut 真为 identity；
3. after-addition 没有破坏 identity 的非线性；
4. branch 参数空间确实包含零函数；
5. normalization state、bias、dropout 和量化没有留下非零变换；
6. 比较时允许旧参数原样嵌入新模型。

例如

$$
x^+=\operatorname{ReLU}(x+F(x))
$$

即使 $F=0$，负坐标也被截断，所以整个 block 不是 $\mathbb R^D$ 上的 identity。它与 pre-activation 的边界在 [[Pre-Activation、Pre-Norm 与 Post-Norm 残差]] 继续处理。

## 七、degradation problem 的逻辑

He et al. 的关键观察不是“测试误差随深度变差”，而是某些更深 plain nets 连训练误差也更高。若更深架构理论上可嵌入较浅解，那么这造成张力：

$$
\text{存在不差的深层参数}
\centernot\Rightarrow
\text{训练算法能到达它}.
$$

因此 degradation evidence 支持“参数化与优化有困难”，但不能单凭它确定唯一机制。初始化、normalization、activation、学习率、宽度、计算预算与实现都可能参与。

## 八、多层 residual stream 的精确展开

若每层都是 identity shortcut，

$$
x_{\ell+1}=x_\ell+F_\ell(x_\ell),
$$

逐层代入得到

$$
\boxed{
x_L=x_0+\sum_{\ell=0}^{L-1}F_\ell(x_\ell)
}.
$$

这是严格恒等式。它说明初始表示以系数 1 出现在最终 residual stream 中。

但 $F_\ell(x_\ell)$ 不是固定向量：每一项的输入依赖前面全部 branch。因此不能由这个求和式推出“深网只是很多互不相关的浅层特征相加”。

## 九、带 projection 的展开

若

$$
x_{\ell+1}=P_\ell x_\ell+F_\ell(x_\ell),
$$

则两层展开为

$$
x_{\ell+2}
=P_{\ell+1}P_\ell x_\ell
+P_{\ell+1}F_\ell(x_\ell)
+F_{\ell+1}(x_{\ell+1}).
$$

初始 state 不再以裸 $I$ 传播，而是乘 projection product。下采样可丢失信息，channel projection 可病态，不能把所有“有 skip 的块”都叫 identity residual block。

## 十、线性玩具：两种参数化

令所有层线性且忽略 bias。

plain 深线性网络：

$$
x_L=W_{L-1}\cdots W_0x_0.
$$

linear residual network：

$$
x_L=(I+A_{L-1})\cdots(I+A_0)x_0.
$$

若目标矩阵 $M$ 接近 $I$，residual 参数可从 $A_\ell\approx0$ 出发；plain 参数仍需多个 $W_\ell$ 的乘积共同构造 $M$。这说明 parameter geometry 改变，但不证明 residual parameterization 对任意远离 identity 的目标都更优。

## 十一、一个标量手算

目标是 $H(x)=1.21x$，用两层共享标量参数。

plain：

$$
y=w^2x.
$$

residual：

$$
y=(1+a)^2x.
$$

精确解分别是

$$
w=1.1,
\qquad
a=0.1.
$$

在 residual 坐标中，identity 目标对应 $a=0$；目标偏离 identity 的 21% 由一个 0.1 增量表达。这个例子只说明局部坐标便利，不说明真实非凸训练速度。

## 十二、zero branch 不等于“所有参数置零”

考虑

$$
F(x)=W_2\phi(W_1x+b_1)+b_2.
$$

令 $W_2=0,b_2=0$ 足以使 branch 输出为零，而把所有参数设零可能造成 hidden units 对称、使早期梯度受阻。若 branch 中有 BatchNorm/LayerNorm、running state、affine bias 或 stochastic operation，必须直接检查实际输出与 VJP，不能靠参数名猜测。

实践中常见“zero-last”是让 branch 最后可学习 scale/weight 为零，使整个 block 从 identity 起步，同时保留内部特征的非退化随机性。

## 十三、残差不是免费的

对 shape-matched addition：

- 参数量通常不因 identity shortcut 增加；
- 加法需要读两路 tensor 并写输出；
- 训练需保存或重算 branch 所需 activation；
- projection shortcut 会增加参数、FLOPs 与 memory traffic；
- residual stream 可能成为 pipeline/tensor-parallel 的通信与 layout 接口。

“shortcut 零计算”是算法草图，不是系统账本。

## 十四、有限精度中的 identity

浮点执行的是

$$
\operatorname{fl}(x+F(x)).
$$

当 $|F_i(x)|$ 小于 $x_i$ 附近约半个 ulp 时，可能出现

$$
\operatorname{fl}(x_i+F_i(x))=x_i.
$$

因此“小 residual”既可能稳定，也可能被吸收。反过来，若 $x$ 与 $F(x)$ 很大且异号，加法可能发生消去。混合精度中应记录 residual accumulation dtype、cast 位置与 fused kernel 语义。

## 十五、图：基准路径、重参数化与证据边界

先看图回答：plain block 与 residual block 分别从什么基准开始？哪一步只是“存在 identity 解”，哪一步才是训练证据？projection shortcut 为什么必须单列？

![[00-知识库管理/_assets/figures/neural-networks/fig-residual-identity-degradation-v2.svg|900]]

> [!figure] 图 30.6-01　残差学习把整映射问题改写为 identity 基线上的增量问题
> 左栏比较 plain $H(x)$ 与 $x+F(x)$ 的目标坐标；中栏展示 identity/zero-branch、projection shortcut 与多层 residual sum；右栏把 function inclusion、optimizer reachability、训练误差和测试性能分成不同证据层。来源：依据 He et al. 2016 两篇论文与本节独立整理；由 [[00-知识库管理/_labs/code/plot_residual_foundations_v2.py]] 确定性生成。

**怎样读图**：先确认 shortcut 是否真为 $I$，再检查 $F=0$ 是否让完整 block 成为 identity，最后沿右栏从“能表示”走到“能优化”和“能泛化”，不要跨级使用结论。

**图没有证明什么**：图没有证明所有目标都接近 identity，也没有证明 residual parameterization 在所有任务、优化器和算力预算下更优。

## 十六、证据等级

| 命题 | 等级 | 所需证据 |
|---|---|---|
| $x^+=x+F(x)$ | 定义 | 计算图 |
| $F=0$ 给 identity | 结构定理 | 完整 block 条件 |
| Jacobian 含 $I$ | 微分恒等式 | 可微/广义导数约定 |
| 深层梯度更稳定 | 有条件理论 | branch Jacobian 与跨层 product |
| 训练更容易 | 经验 | 配对基线、多 seed、预算 |
| 测试更好 | 经验/统计 | 数据、调参、公平评估 |

## 十七、可执行审计

对任一“ResidualBlock”实现，逐项记录：

1. input/output shape；
2. shortcut 是 $I$、projection、gate 还是缩放；
3. branch scale 与初始化；
4. addition 前后 activation/norm；
5. $F=0$ 时实际输出；
6. train/eval 与 stochastic state；
7. accumulation dtype；
8. branch/output RMS 比；
9. optimizer update-to-weight ratio；
10. 对照实验是否匹配 width、参数量、训练 tokens 与调参预算。

## 十八、最小验收

1. 从 $H$ 写出 residual target；
2. 给出 identity 与 projection shortcut 的 shape；
3. 解释 degradation 与 overfitting 的差别；
4. 列出函数类嵌套的六个条件；
5. 展开三层 residual stream；
6. 给出 post-add ReLU 的 identity 反例；
7. 比较 $W_L\cdots W_1$ 与 $\prod(I+A_\ell)$；
8. 解释 zero-last 与全零初始化的差别；
9. 给出 residual absorption 的浮点边界；
10. 把“可表示、可优化、可泛化”分别归类。

> [!summary]
> 残差学习的第一性作用是提供显式 identity 基线：block 学习偏离基线的增量。它让“新增层不改函数”在满足条件时成为可实现参数设置，并让信息可以沿 shortcut 传播；但从这个结构事实到深网训练与测试优势，中间仍隔着 Jacobian、优化轨迹、数值系统和统计证据。

- [[残差连接、深度与稳定性 MOC]]
- [[习题 - 残差学习、恒等捷径与退化问题]]
- [[解答 - 残差学习、恒等捷径与退化问题]]
