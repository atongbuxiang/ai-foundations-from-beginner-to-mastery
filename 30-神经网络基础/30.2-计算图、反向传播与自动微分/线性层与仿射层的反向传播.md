---
type: concept
status: draft
area: [neural-networks/backpropagation, dense-layer, matrix-calculus]
aliases: [Dense Layer Backward, Affine Layer Backprop]
node_id: NN-12
prerequisites: ["[[线性层、批量张量与参数计数]]", "[[标量链式法则与反向传播递推]]", "[[矩阵微分、迹技巧与布局约定]]"]
related: ["[[激活、分支、广播与梯度累加]]", "[[Softmax–Cross-Entropy 的稳定融合反向]]", "[[稳定求和、点积与矩阵乘法]]"]
sources: ["[[S-2016-Goodfellow-Bengio-Courville-Deep-Learning]]", "[[S-2023-Zhang-Lipton-Li-Smola-D2L]]", "[[S-1986-Rumelhart-Hinton-Williams-Backprop]]"]
exercises: ["[[习题 - 线性层与仿射层的反向传播]]"]
solutions: ["[[解答 - 线性层与仿射层的反向传播]]"]
figure: "[[00-知识库管理/_assets/figures/neural-networks/fig-dense-layer-backward-v2.svg]]"
created: 2026-08-23
updated: 2026-08-29
---
# 线性层与仿射层的反向传播

> [!abstract] 本章主问题
> 对 row-batch affine layer $Z=XW+\mathbf1b$，给定 upstream cotangent $G=\partial L/\partial Z$，三个 VJP 是 $\bar X=GW^T$、$\bar W=X^TG$、$\bar b=\mathbf1^TG$。公式来自同一个微分与 Frobenius 配对；transpose 和 reduction 都由形状与线性算子伴随强制决定。

## 课程位置与两遍学习路线

- **承接什么：** NN-11 已规定 reverse 如何调度和累加，但 affine operation 内部仍需要一个精确、可复用的局部 VJP；
- **本页解决什么：** 从一次总微分与 Frobenius pairing 同时推出 input、weight 和 bias 三路梯度，并接回贯穿任务的完整数值；
- **后续为何需要：** NN-13 会把 activation、branch 和 broadcast 继续接入图，NN-14 会展示 softmax–cross-entropy 为什么应融合求导与稳定计算。

**第一遍只用 shape 推公式。** 先固定 row-batch convention，给定 $G$ 后逐项写出与 $X,W,b$ 同形的结果；不背转置方向，靠内维匹配检查。

**第二遍再用微分证明。** 从 $dZ=dXW+XdW+\mathbf1db$ 出发，用 trace/Frobenius pairing 隔离每个 differential，并审计 mean/sum、序列轴、低精度 reduction 与分布式缩放。

### 问题链

1. 一个 affine forward 为什么产生三个不同目标的 VJP？
2. $W^T$、$X^T$ 与 batch sum 分别由哪个线性算子的 adjoint 强制出现？
3. upstream $G$ 是对 $Z$ 还是对 activation 后 $H$ 的 cotangent？
4. per-example outer products 怎样合成 batch parameter gradient？
5. loss 使用 sum、mean 或 microbatch accumulation 时，缩放应在哪一层处理？

> [!check] 第一遍停靠线
> 若你能只看 shape 写出 $\bar X=GW^T$、$\bar W=X^TG$、$\bar b=\sum_iG_i$，并在贯穿算例算出三者，就可以进入 NN-13；trace 证明和系统缩放留到第二遍。

## 符号与对象账本

| 对象 | shape | 在 AI 模型中的角色 | reverse 结果 |
|---|---|---|---|
| $X$ | $B\times d_{\rm in}$ | batch inputs/activations | $\bar X=GW^T$ |
| $W$ | $d_{\rm in}\times d_{\rm out}$ | shared linear parameters | $\bar W_{\rm data}=X^TG$ |
| $b$ | $1\times d_{\rm out}$ | broadcast parameter | $\bar b=\mathbf1_B^TG$ |
| $Z$ | $B\times d_{\rm out}$ | pre-activation | receives upstream $G=\bar Z$ |
| $G$ | $B\times d_{\rm out}$ | output cotangent | 不是另一个 primal batch |

### 贯穿算例：三路 affine VJP 与正则贡献闭合

沿用 $X_\diamond,W_\diamond,b_\diamond,Y_\diamond$。平方损失给

$$
G=R=\begin{bmatrix}1&1\\-1&2\end{bmatrix}.
$$

于是

$$
\bar X=GW_\diamond^T=\begin{bmatrix}0&3\\-3&0\end{bmatrix},\qquad
\bar W_{\rm data}=X_\diamond^TG=\begin{bmatrix}2&-1\\1&4\end{bmatrix},
$$

$$
\bar b=\mathbf1^TG=\begin{bmatrix}0&3\end{bmatrix}.
$$

再合并 NN-11 的 $\bar W_{\rm reg}=\lambda W_\diamond$，得到总梯度 $\bar W=\left[\begin{smallmatrix}2.5&-1.5\\2&4.5\end{smallmatrix}\right]$。NN-10 选择的方向 $\dot W_{11}=1$ 因而读取 directional derivative $\langle\bar W,\dot W\rangle_F=2.5$，四页数值闭合。

## 核心公式七问：$(\bar X,\bar W,\bar b)=(GW^T,X^TG,\mathbf1^TG)$

| 问题 | 本式的回答 |
|---|---|
| 目的 | 不形成巨大 Jacobian，直接计算 affine primitive 对全部输入的局部 VJP |
| 对象 | upstream $G$ 与 $Z$ 同形，三个输出 cotangents 分别与 $X,W,b$ 同形 |
| 来路 | 将 $dZ=dXW+XdW+\mathbf1db$ 代入 $dL=\langle G,dZ\rangle_F$ 并移动因子 |
| 步骤 | 每一项只隔离一个 differential；共享/正则产生的额外参数贡献在节点外累加 |
| 读法 | $GW^T$ 回拉 feature，$X^TG$ 聚合 input–error outer products，$\mathbf1^TG$ 撤销 broadcast |
| 检查 | $B=1$ 退化为单样本 outer product；复制 batch 时 sum loss 梯度倍增、mean loss不变 |
| 去路 | activation VJP、convolution/attention matmul backward、per-example gradients 与 all-reduce |

## 一、前向与形状
$$X:[B,d_{in}],\quad W:[d_{in},d_{out}],\quad b:[d_{out}],$$
$$Z=XW+\mathbf1_Bb:[B,d_{out}].$$
令 upstream $G=\bar Z$ 与 $Z$ 同形。

## 二、微分一次写全
$$dZ=dXW+XdW+\mathbf1_Bdb.$$
标量 loss 的 differential 为
$$dL=\langle G,dZ\rangle_F=\operatorname{tr}(G^TdZ).$$
分别隔离 $dX,dW,db$ 即可读取 gradients。

## 三、输入梯度
$$\operatorname{tr}(G^TdXW)=\operatorname{tr}((GW^T)^TdX),$$
所以
$$\boxed{\bar X=GW^T:[B,d_{in}].}$$
它把 output-feature cotangent 经 weight transpose 拉回 input-feature space。

## 四、权重梯度
$$\operatorname{tr}(G^TXdW)=\operatorname{tr}((X^TG)^TdW),$$
所以
$$\boxed{\bar W=X^TG:[d_{in},d_{out}].}$$
分量式
$$\bar W_{kj}=\sum_{i=1}^BX_{ik}G_{ij}$$
表明每个样本贡献 input activation 与 output error signal 的 outer product，batch 上求和。

## 五、Bias 梯度
$$\boxed{\bar b=\mathbf1_B^TG=\sum_{i=1}^BG_{i,:}:[d_{out}].}$$
forward broadcasting 的 transpose 是沿广播轴 reduction。若输入还有 time/spatial axes，共享 bias gradient 对所有这些位置求和。

## 六、完整数值手算
取
$$X=\begin{bmatrix}1&2\\-1&3\end{bmatrix},\quad
W=\begin{bmatrix}2&0\\1&-1\end{bmatrix},\quad
G=\begin{bmatrix}1&-2\\3&1\end{bmatrix}.$$
则
$$\bar X=GW^T=\begin{bmatrix}2&3\\6&2\end{bmatrix},$$
$$\bar W=X^TG=\begin{bmatrix}-2&-3\\11&-1\end{bmatrix},$$
$$\bar b=(4,-1).$$
三者分别与 $X,W,b$ 同形。

## 七、逐样本 Outer Product 解释
单样本 $x_i:[1,d_{in}]$、$g_i:[1,d_{out}]$ 的 weight contribution 是
$$x_i^Tg_i:[d_{in},d_{out}].$$
batch gradient为 $\sum_i x_i^Tg_i$。这解释了 microbatch accumulation、data parallel all-reduce 和 per-example gradient 的关系。

## 八、Sum 与 Mean Loss
若 $L_{mean}=B^{-1}\sum_i\ell_i$，则 $G$ 已包含 $1/B$，上述公式不再额外除一次。若 framework loss 使用 sum，gradient 会大 $B$ 倍。gradient accumulation 跨 microbatches 时必须先确定要复现 global sum 还是 global mean。

## 九、序列/空间张量
对 $X:[B,T,d_{in}]$，可把前两轴展平为 $N=BT$：
$$\bar W=\sum_{b,t}X_{bt,:}^TG_{bt,:},\qquad \bar b=\sum_{b,t}G_{bt,:}.$$
$\bar X_{bt,:}=G_{bt,:}W^T$，不会跨 token 混合；参数共享只在 parameter gradients 中聚合。

## 十、接 Activation
若 $H=\phi(Z)$，先由上游 $\bar H$ 得
$$G=\bar Z=\bar H\odot\phi'(Z),$$
再代入 affine backward。必须区分 upstream 对 $H$ 的 gradient 与经过 activation VJP 后对 $Z$ 的 gradient。

## 十一、Column-vector Convention
若单样本写 $z=Wx+b$，则 $W:[d_{out},d_{in}]$，公式形式相应为 $\bar x=W^T\bar z$、$\bar W=\bar z x^T$。它与 row convention 等价，但不可在一次推导中混用。

## 十二、框架 Storage
PyTorch `Linear` 常存 weight `[out,in]`，其 forward 相当于 $XW_{store}^T+b$。autograd 返回与 storage 同形的 gradient。比较数学公式前必须先转成共同 layout。

## 十三、数值与系统边界
- low-precision $X^TG$ reduction 可能积累误差；
- large $BT$ 使 bias/weight gradient overflow；
- fused kernels 可能改变加法顺序；
- sparse/quantized forward 的 backward 可能使用 surrogate 或 higher precision；
- distributed all-reduce 的 scale 必须与 loss reduction 一致。

## 十四、验证协议
1. small integer hand case；
2. shape equality；
3. finite difference 对 $X,W,b$ 各随机方向；
4. dot test 比较 forward linearization 与 VJP；
5. batch duplication test：sum loss 应倍增 gradient，mean loss 应保持；
6. broadcast axes test。

## 十五、图：三个梯度来自一条微分
先看图回答：为什么 $W^T$、$X^T$ 和 batch sum 不是记忆口诀，而是同一个 forward linear operator 的三个伴随？

![[00-知识库管理/_assets/figures/neural-networks/fig-dense-layer-backward-v2.svg|900]]

> [!figure] 图 30.2-04　Affine layer 的微分、三路 VJP 与 reduction 语义
> 左栏写 $dZ$ 三项；中栏以 shape ledger 推出 $GW^T,X^TG,\sum G$；右栏连接 per-example outer products、mean/sum、sequence sharing 与 distributed reduction。来源：依据 Goodfellow–Bengio–Courville、D2L 及本库矩阵微分规范独立绘制；由 [[00-知识库管理/_labs/code/plot_backprop_foundations_v2.py]] 确定性生成。

**怎样读图**：先固定 row-batch layout，再按 Frobenius pairing 隔离每个 differential，最后检查 reduction 轴和 loss scale。

**图没有证明什么**：图没有保证低精度 reduction 稳定、fused/distributed 实现 bitwise 等价，或自定义量化 backward 等于真实 forward derivative。

## 十六、回顾与练习
> [!summary]
> $dZ=dXW+XdW+1db$；$\bar X=GW^T$，$\bar W=X^TG$，$\bar b=\sum G$。transpose 与 sum 都由伴随和 shape 强制决定。

- [[习题 - 线性层与仿射层的反向传播]]
- [[解答 - 线性层与仿射层的反向传播]]
