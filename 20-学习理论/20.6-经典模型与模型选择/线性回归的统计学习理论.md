---
type: theorem
status: draft
area: [learning-theory/linear-regression, random-design, least-squares, misspecification]
aliases: [Statistical Theory of Linear Regression, OLS Learning Theory, Random Design Regression]
node_id: LT-43
prerequisites: ["[[最小二乘]]", "[[统计模型、估计量与偏差方差]]", "[[多元高斯分布]]", "[[稳定最小二乘与正规方程的风险]]"]
related: ["[[偏差—方差—噪声分解]]", "[[正则化、交叉验证与模型选择]]", "[[核岭回归与 Gaussian Process 接口]]", "[[模型可辨识性、选择与 Misspecification]]"]
sources: ["[[S-2009-Hastie-Tibshirani-Friedman-ESL]]", "[[S-1980-White-Heteroskedasticity-Covariance]]", "[[S-2020-Rosset-Tibshirani-Fixed-Random-X]]"]
exercises: ["[[习题 - 线性回归的统计学习理论]]"]
solutions: ["[[解答 - 线性回归的统计学习理论]]"]
figure: "[[00-知识库管理/_assets/figures/learning-theory/fig-linear-regression-statistical-contract-v2.svg]]"
created: 2026-08-23
updated: 2026-08-23
---

# 线性回归的统计学习理论

> [!abstract] 本章主问题
> linear regression 有三个不能混写的对象：
>
> 1. population best linear predictor
>    $$
>    \beta^\star
>    \in\arg\min_\beta
>    \mathbb E(Y-X^\top\beta)^2;
>    $$
> 2. fixed-design OLS estimator
>    $$
>    \widehat\beta=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top y;
>    $$
> 3. Random-X prediction risk
>    $$
>    \|\widehat\beta-\beta^\star\|_\Sigma^2.
>    $$
>
> 在正确同方差线性模型下，OLS conditional unbiased，covariance 是 \(\sigma^2(\mathbf X^\top\mathbf X)^{-1}\)。但 Gauss–Markov 只比较 linear unbiased estimators，不说明 OLS 在 MSE 上压过 ridge；heteroskedasticity 改变 covariance；misspecification 把目标变为 linear \(L^2\) projection；\(d\ge n\) 时 inverse、唯一性和经典 inference 全部需要重写。

> [!question] 初学者读完必须能回答
> 1. population linear projection 与“真实线性因果机制”有什么差别？
> 2. \(E[XU]=0\) 为什么弱于 \(E[U\mid X]=0\)？
> 3. Gauss–Markov 的 BLUE 究竟在哪个比较集合里？
> 4. 同方差、异方差、Gaussian noise 分别用于哪一步？
> 5. fixed-\(X\) coefficient covariance 怎样连接 Random-\(X\) excess prediction risk？
> 6. rank deficiency、collinearity 与 numerical instability 怎样分层？

## 一、学习目标

1. 从 population squared risk 推导 normal equations 与 linear projection；
2. 区分 well-specified conditional mean 与 best linear approximation；
3. 推导 fixed-design OLS mean、covariance 与 prediction variance；
4. 证明 Gauss–Markov BLUE；
5. 区分 confidence interval 与 prediction interval；
6. 推导 Gaussian random design 下 exact expected excess risk；
7. 处理 heteroskedastic sandwich covariance 与 misspecification；
8. 用 SVD 解释 collinearity、rank deficiency、ridge 与 minimum norm；
9. 区分 statistical conditioning 与 floating-point stability；
10. 审计 linear probe、last-layer regression 与 high-dimensional representation。

## 二、Notation 与 Intercept

单个 feature vector：

$$
X\in\mathbb R^d,
\qquad
Y\in\mathbb R.
$$

若需要 intercept，可把第一 coordinate 设为 \(1\)：

$$
X=(1,\widetilde X^\top)^\top.
$$

训练 design matrix：

$$
\mathbf X
=
\begin{bmatrix}
X_1^\top\\
\vdots\\
X_n^\top
\end{bmatrix}
\in\mathbb R^{n\times d},
\qquad
y=(Y_1,\ldots,Y_n)^\top.
$$

本章用大写 random vector \(X\) 与粗体 realized matrix \(\mathbf X\) 分开。

## 三、Population Best Linear Predictor

定义 population squared risk：

$$
R(\beta)
=
\mathbb E(Y-X^\top\beta)^2.
$$

设

$$
\Sigma=\mathbb E[XX^\top],
\qquad
\mathbb E[Y^2]<\infty.
$$

展开：

$$
R(\beta)
=
\mathbb E[Y^2]
-2\beta^\top\mathbb E[XY]
+\beta^\top\Sigma\beta.
$$

若 \(\Sigma\succ0\)，梯度为

$$
\nabla R(\beta)
=
-2\mathbb E[XY]+2\Sigma\beta.
$$

所以唯一 population minimizer：

$$
\boxed{
\beta^\star
=
\Sigma^{-1}\mathbb E[XY].}
$$

normal equation：

$$
\boxed{
\mathbb E[
X(Y-X^\top\beta^\star)
]=0.}
$$

令 population residual

$$
U=Y-X^\top\beta^\star.
$$

则

$$
\mathbb E[XU]=0.
$$

这叫 residual 与 linear feature span 正交。

## 四、Projection Target 不等于真实条件均值

真实 squared-loss Bayes function：

$$
f^\star(x)=\mathbb E[Y\mid X=x].
$$

若存在 \(\beta_0\) 使

$$
f^\star(X)=X^\top\beta_0
\quad\text{a.s.},
$$

则 model well-specified，且

$$
\beta^\star=\beta_0,
\qquad
\mathbb E[U\mid X]=0.
$$

若 conditional mean 非线性，\(\beta^\star\) 仍可定义为 best linear approximation，但一般只有

$$
\mathbb E[XU]=0,
$$

并不保证

$$
\mathbb E[U\mid X]=0.
$$

### 4.1 最小反例

若

$$
X\sim\operatorname{Unif}[-1,1],
\qquad
Y=X^2,
$$

并只使用 linear feature \(X\) 而没有 intercept。对称性给

$$
\mathbb E[XY]
=\mathbb E[X^3]
=0,
$$

所以 \(\beta^\star=0\)，但

$$
\mathbb E[U\mid X]
=X^2\ne0.
$$

linear projection 存在，不代表 mechanism 线性。

## 五、Risk 的 Pythagorean Identity

对任意 \(\beta\)：

$$
Y-X^\top\beta
=
U+X^\top(\beta^\star-\beta).
$$

平方取期望：

$$
\begin{aligned}
R(\beta)
&=
\mathbb E[U^2]
+\mathbb E[X^\top(\beta-\beta^\star)]^2\\
&\quad
-2(\beta-\beta^\star)^\top\mathbb E[XU].
\end{aligned}
$$

normal equation 使 cross term 为零：

$$
\boxed{
R(\beta)-R(\beta^\star)
=
(\beta-\beta^\star)^\top
\Sigma
(\beta-\beta^\star)
=
\|\beta-\beta^\star\|_\Sigma^2.}
$$

因此 coefficient Euclidean error 不是 prediction excess risk 的唯一自然 metric；\(\Sigma\) 会按 deployment feature directions 加权。

## 六、图解：Projection、Fixed-X 与 Random-X

先回答：**为什么右栏把 \(\Sigma\)-norm 而不是 \(\ell_2\)-norm 写成新输入风险？**

![[00-知识库管理/_assets/figures/learning-theory/fig-linear-regression-statistical-contract-v2.svg|900]]

> [!figure] 图 20.6.3｜总体线性投影、fixed-design sampling covariance 与谱/异方差/Random-X 风险
> 左栏把 best linear predictor 表成 \(L^2\) 投影；中栏给出 fixed-\(X\) OLS 的 conditional covariance 与新点 mean uncertainty；右栏连接 singular directions、heteroskedastic sandwich 与 \(\Sigma\)-weighted Random-X excess risk。来源：依据 ESL、White 与 Rosset–Tibshirani 独立绘制；确定性 SVG，由 [[plot_classical_models_core_v2.py]] 生成。

**怎样读图。** QR/SVD 解决“怎样稳定算出指定 estimator”，sandwich covariance 解决“noise covariance 不同后怎样做 inference”，Random-X norm 解决“新 feature distribution 怎样加权 parameter error”。三者不能互相替代。

**图没有证明什么。** 它没有覆盖 endogenous regressors、dependent clusters、selection-after-inference、heavy tails、\(d\ge n\) 的现代 benign-overfitting theorem 或因果识别。

## 七、Fixed-Design Linear Model

给定 deterministic/full-rank design \(\mathbf X\)，假设

$$
y=\mathbf X\beta_0+\varepsilon,
$$

$$
\mathbb E[\varepsilon\mid\mathbf X]=0.
$$

OLS：

$$
\widehat\beta
=
(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top y.
$$

代入模型：

$$
\widehat\beta-\beta_0
=
(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top\varepsilon.
$$

conditional mean：

$$
\boxed{
\mathbb E[\widehat\beta\mid\mathbf X]
=\beta_0.}
$$

这里不需要 Gaussian noise，只需要 conditional mean zero。

## 八、General Conditional Covariance

令

$$
\Omega
=
\operatorname{Cov}(\varepsilon\mid\mathbf X).
$$

则

$$
\boxed{
\operatorname{Cov}(\widehat\beta\mid\mathbf X)
=
(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top\Omega\mathbf X
(\mathbf X^\top\mathbf X)^{-1}.}
$$

若 homoskedastic uncorrelated：

$$
\Omega=\sigma^2I_n,
$$

才化简为

$$
\boxed{
\operatorname{Cov}(\widehat\beta\mid\mathbf X)
=
\sigma^2(\mathbf X^\top\mathbf X)^{-1}.}
$$

mean-zero 用于 unbiasedness；\(\Omega=\sigma^2I\) 用于简单 covariance。两者是不同假设。

## 九、Gauss–Markov：BLUE 的精确含义

考虑任意 linear estimator：

$$
\widetilde\beta=Cy.
$$

对所有 \(\beta_0\) unbiased 要求：

$$
C\mathbf X=I_d.
$$

OLS matrix：

$$
C_0
=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top.
$$

写

$$
C=C_0+D.
$$

由 unbiasedness：

$$
D\mathbf X=0.
$$

在 \(\Omega=\sigma^2I\) 下：

$$
\operatorname{Cov}(\widetilde\beta\mid\mathbf X)
=\sigma^2CC^\top.
$$

cross term：

$$
C_0D^\top
=(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top D^\top
=(\mathbf X^\top\mathbf X)^{-1}
(D\mathbf X)^\top
=0.
$$

所以

$$
\boxed{
\operatorname{Cov}(\widetilde\beta\mid\mathbf X)
-
\operatorname{Cov}(\widehat\beta\mid\mathbf X)
=
\sigma^2DD^\top
\succeq0.}
$$

### 9.1 BLUE 没有说什么

Best Linear Unbiased Estimator 只在：

- linear in \(y\)；
- unbiased for all \(\beta_0\)；
- fixed \(\mathbf X\)；
- homoskedastic uncorrelated errors

的集合内比较 covariance。

它没有说 OLS：

- 在 biased estimators 中 MSE 最小；
- 优于 ridge；
- 在 nonlinear estimators 中 minimax；
- 在 heteroskedasticity 下 efficient；
- 在 high dimension 中唯一；
- 在 misspecification 下恢复 causal coefficient。

## 十、Gaussian Noise 增加了什么

若

$$
\varepsilon\mid\mathbf X
\sim\mathcal N(0,\sigma^2I),
$$

则

$$
\widehat\beta\mid\mathbf X
\sim
\mathcal N(
\beta_0,
\sigma^2(\mathbf X^\top\mathbf X)^{-1}
).
$$

Gaussian assumption 用于 finite-sample exact distribution、\(t\)-interval 与 \(F\)-test。它不是 OLS unbiasedness 或 Gauss–Markov 所必需。

residual variance estimator：

$$
\widehat\sigma^2
=
\frac{\|y-\mathbf X\widehat\beta\|_2^2}{n-d}
$$

在 full-rank correct Gaussian model 下与 \(\widehat\beta\) 有经典 independence，并支持 exact inference。

## 十一、Mean Confidence 与 New-Response Prediction

对 fixed test feature \(x_0\)，estimated conditional mean：

$$
\widehat\mu_0=x_0^\top\widehat\beta.
$$

conditional variance：

$$
\operatorname{Var}(\widehat\mu_0\mid\mathbf X)
=
\sigma^2
x_0^\top(\mathbf X^\top\mathbf X)^{-1}x_0.
$$

fresh response：

$$
Y_0=x_0^\top\beta_0+\varepsilon_0
$$

还包含 test noise：

$$
\operatorname{Var}(Y_0-\widehat\mu_0\mid\mathbf X,x_0)
=
\sigma^2
\left[
1+x_0^\top(\mathbf X^\top\mathbf X)^{-1}x_0
\right].
$$

所以 prediction interval 比 mean-response confidence interval 更宽。两者都假设 \(x_0\) 在相同 model/data regime 下。

## 十二、Hat Matrix、Leverage 与 Residual

fitted values：

$$
\widehat y
=
\mathbf X\widehat\beta
=
Hy,
$$

其中

$$
H
=
\mathbf X
(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top.
$$

\(H\) 是投影矩阵：

$$
H^\top=H,
\qquad
H^2=H,
\qquad
\operatorname{tr}(H)=d.
$$

leverage：

$$
h_{ii}=H_{ii}.
$$

它衡量 observation \(i\) 的 feature location 对 fitted value 的自影响，不等于 residual 大小。

### 12.1 LOOCV Shortcut

对 exact OLS：

$$
\boxed{
Y_i-\widehat Y_i^{(-i)}
=
\frac{e_i}{1-h_{ii}},
}
$$

其中

$$
e_i=Y_i-\widehat Y_i.
$$

高 leverage \(h_{ii}\approx1\) 会放大 leave-one-out residual。这个 shortcut 不可直接用于 arbitrary nonlinear/regularized pipeline。

## 十三、Random-Design Excess Prediction Risk

现在训练 rows \(X_i\) i.i.d.，新 \(X_0\sim P_X\) 独立。设 conditional mean well-specified：

$$
Y=X^\top\beta_0+\varepsilon,
\qquad
\mathbb E[\varepsilon\mid X]=0.
$$

令

$$
\Sigma=\mathbb E[XX^\top].
$$

conditional on training data，new-input mean excess：

$$
\begin{aligned}
&\mathbb E_{X_0}
\left[
(X_0^\top\widehat\beta-X_0^\top\beta_0)^2
\middle|\widehat\beta
\right]\\
&=
(\widehat\beta-\beta_0)^\top
\Sigma
(\widehat\beta-\beta_0)\\
&=
\boxed{
\|\widehat\beta-\beta_0\|_\Sigma^2.}
\end{aligned}
$$

fresh-response risk 再加

$$
\mathbb E[\operatorname{Var}(Y_0\mid X_0)].
$$

## 十四、Gaussian Random Design 的 Exact Expected Rate

设：

$$
X_i\overset{\rm iid}{\sim}\mathcal N(0,\Sigma),
\qquad
\Sigma\succ0,
$$

$$
\operatorname{Var}(\varepsilon_i\mid X_i)=\sigma^2,
\qquad
n>d+1.
$$

conditional on \(\mathbf X\)：

$$
\mathbb E[
\|\widehat\beta-\beta_0\|_\Sigma^2
\mid\mathbf X
]
=
\sigma^2
\operatorname{tr}
\left[
\Sigma(\mathbf X^\top\mathbf X)^{-1}
\right].
$$

因为

$$
\mathbf X^\top\mathbf X
\sim
\operatorname{Wishart}_d(\Sigma,n)
$$

且

$$
\mathbb E[
(\mathbf X^\top\mathbf X)^{-1}
]
=
\frac{\Sigma^{-1}}{n-d-1},
$$

所以

$$
\boxed{
\mathbb E
\|\widehat\beta-\beta_0\|_\Sigma^2
=
\frac{\sigma^2d}{n-d-1}.}
$$

当 \(d\) 接近 \(n\)，risk 激增。固定 \(d\)、\(n\to\infty\) 时约为

$$
\frac{\sigma^2d}{n}.
$$

这是带 Gaussian-design 条件的 exact result，不应把 \(d/n\) 常数无条件移植到 heavy-tail、dependent 或 singular designs。

## 十五、Heteroskedasticity

若

$$
\mathbb E[\varepsilon_i\mid X_i]=0,
\qquad
\operatorname{Var}(\varepsilon_i\mid X_i)=\sigma_i^2,
$$

OLS 仍可 conditional unbiased，但 covariance 是

$$
(\mathbf X^\top\mathbf X)^{-1}
\mathbf X^\top
\operatorname{diag}(\sigma_1^2,\ldots,\sigma_n^2)
\mathbf X
(\mathbf X^\top\mathbf X)^{-1}.
$$

naive 同方差 formula：

$$
\widehat\sigma^2
(\mathbf X^\top\mathbf X)^{-1}
$$

一般错误。

### 15.1 White Sandwich

令 residual：

$$
\widehat u_i=Y_i-X_i^\top\widehat\beta.
$$

HC0-type estimator：

$$
\boxed{
\widehat{\operatorname{Cov}}_{\rm HC0}
(\widehat\beta)
=
(\mathbf X^\top\mathbf X)^{-1}
\left[
\sum_{i=1}^n
X_iX_i^\top\widehat u_i^2
\right]
(\mathbf X^\top\mathbf X)^{-1}.}
$$

它在适当 regularity 下 asymptotically consistent。它不自动修复：

- small-sample leverage bias；
- clustered/time dependence；
- misspecified conditional mean；
- endogenous \(E[X\varepsilon]\ne0\)；
- numerical rank deficiency。

## 十六、Misspecification 与 Pseudo-True Target

即使

$$
\mathbb E[Y\mid X]
\ne
X^\top\beta
\quad\forall\beta,
$$

population projection \(\beta^\star\) 仍由

$$
\mathbb E[X(Y-X^\top\beta^\star)]=0
$$

定义。

under i.i.d. moments and nonsingular

$$
A=\mathbb E[XX^\top],
$$

OLS 可收敛到 \(\beta^\star\)。其 asymptotic covariance 是 sandwich：

$$
\boxed{
\sqrt n(\widehat\beta-\beta^\star)
\rightsquigarrow
\mathcal N(
0,
A^{-1}BA^{-1}
),
}
$$

其中

$$
B
=
\mathbb E[
XX^\top U^2
],
\qquad
U=Y-X^\top\beta^\star.
$$

只有在

$$
\mathbb E[U^2\mid X]=\sigma^2
$$

时，

$$
B=\sigma^2A
$$

并化简为 \(\sigma^2A^{-1}\)。

## 十七、Collinearity 与可辨识性

若 \(\mathbf X^\top\mathbf X\) 有很小 eigenvalue：

- 对应 coefficient direction variance 很大；
- parameter estimates 对 noise 敏感；
- signs/individual coefficients 不稳定；
- training fitted values 仍可能相对稳定。

如果 \(v\) 满足

$$
\mathbf Xv=0,
$$

则

$$
\mathbf X(\beta+tv)=\mathbf X\beta
$$

对任意 \(t\) 相同。training predictions 无法辨识 null-space coefficient。

新输入若也几乎不含该 direction，prediction risk 可能小；若 deployment \(X_0\) 在该 direction 有 variance，Random-X risk 可很大。可辨识性必须相对 data support。

## 十八、\(d\ge n\)：经典 Inverse 失效

若 \(d>n\)，\(\mathbf X^\top\mathbf X\) singular，OLS minimizers 通常不唯一。

minimum-norm interpolator：

$$
\widehat\beta_{\min}
=
\mathbf X^\dagger y
$$

由 Moore–Penrose pseudoinverse 选择 row-space solution。

这引入新的 implicit bias：

- null-space component 设为零；
- result 依 feature parameterization/metric；
- classical \((\mathbf X^\top\mathbf X)^{-1}\) covariance 不存在；
- risk 取决于 spectrum、signal alignment 与 noise；
- benign overfitting 需要专门 random-matrix/distribution assumptions。

不能把 \(d<n\) 的 \(t\)-interval 和 Gauss–Markov 结论机械延伸。

## 十九、Ridge Regression

ridge：

$$
\widehat\beta_\lambda
=
(\mathbf X^\top\mathbf X+\lambda I)^{-1}
\mathbf X^\top y.
$$

在 correct fixed-design model 下：

$$
\operatorname{Bias}
(\widehat\beta_\lambda\mid\mathbf X)
=
-\lambda
(\mathbf X^\top\mathbf X+\lambda I)^{-1}
\beta_0,
$$

$$
\operatorname{Cov}
(\widehat\beta_\lambda\mid\mathbf X)
=
\sigma^2
(\mathbf X^\top\mathbf X+\lambda I)^{-1}
\mathbf X^\top\mathbf X
(\mathbf X^\top\mathbf X+\lambda I)^{-1}.
$$

ridge 不与 OLS 争夺 BLUE，因为它有 bias。其 MSE/prediction risk 可能更低，尤其在小 singular values 与 moderate signal 下。

intercept 是否 penalty、features 是否 standardized、loss 用 sum/mean 都改变 \(\lambda\) 含义。

## 二十、Statistical 与 Numerical Stability

normal equations：

$$
\mathbf X^\top\mathbf X\widehat\beta
=
\mathbf X^\top y
$$

在 exact arithmetic 下正确，但

$$
\kappa(\mathbf X^\top\mathbf X)
\approx
\kappa(\mathbf X)^2.
$$

因此计算上优先：

- Householder QR：full-rank stable least squares；
- pivoted QR：rank revelation；
- SVD：rank-deficient/minimum-norm/regularization；
- iterative methods：large sparse problems，需 residual/conditioning audit。

数值稳定算法只能准确求解 chosen empirical problem；它不能修复:

- omitted variables；
- wrong sample；
- heteroskedastic standard errors；
- distribution shift；
- causal confounding。

## 二十一、Feature Learning 与 Linear Probe

设 representation：

$$
\phi_\omega(X)\in\mathbb R^d
$$

再做 linear regression：

$$
Y\approx\phi_\omega(X)^\top\beta.
$$

若 \(\omega\) 用同一 labels/validation data 学得，design matrix 不再是外生 fixed feature map。完整 estimator 是

$$
S\mapsto\omega_S\mapsto\mathbf\Phi_S\mapsto\widehat\beta_S.
$$

classical OLS conditional covariance 只描述“给定 realized \(\Phi\)”的最后一层 uncertainty，不含 representation-learning variability。

### 21.1 Probe Comparison

比较两个 representations 时应固定：

- probe regularization；
- preprocessing；
- training sample；
- CV protocol；
- effective rank；
- metric；
- seed budget。

否则 probe performance 差异可能来自 condition number 或 selection effort，而不是 representation sufficiency。

## 二十二、因果解释边界

linear coefficient：

$$
\beta^\star
=
\arg\min_\beta E(Y-X^\top\beta)^2
$$

是 observational projection。

要解释为 causal effect，需要额外：

- treatment/outcome definition；
- no unmeasured confounding 或 randomization；
- positivity；
- consistency/SUTVA；
- correct adjustment set；
- interference/selection audit。

高 \(R^2\)、unbiased OLS 或 small p-value 都不能替代 causal identification。

## 二十三、常见误区

> [!warning] 误区 1：normal equation 成立，所以 \(E[Y\mid X]\) 线性
> normal equation 只说明 residual 与 chosen linear features 正交。

> [!warning] 误区 2：Gauss–Markov 证明 OLS 在所有 estimators 中最好
> 它只在 linear unbiased class、同方差 fixed design 内比较 covariance。

> [!warning] 误区 3：robust standard error 修复了模型错误
> sandwich 只修正特定 covariance inference，不恢复错误 conditional mean 或 endogeneity。

> [!warning] 误区 4：coefficient 不稳定意味着 prediction 一定不稳定
> 若 deployment features 不激活弱方向，prediction 可稳定；反之也可能失败。

> [!warning] 误区 5：QR/SVD 算得准，所以统计结论可靠
> computation、sampling、model 与 causal validity 是四层。

## 二十四、验收清单

1. target 是 conditional mean 还是 best linear projection？
2. intercept 如何处理？
3. \(\Sigma\) 是否 nonsingular？
4. fixed design 还是 random design？
5. conditional mean zero 是否可信？
6. homoskedasticity 是否只在需要处使用？
7. Gaussian assumption 用于 exact inference 还是被多余添加？
8. coefficient error 用 \(\ell_2\) 还是 prediction \(\Sigma\)-norm？
9. rank、singular values 与 leverage 怎样？
10. \(d<n\) 还是 \(d\ge n\)？
11. covariance 用 naive、HC、cluster 还是 time-series version？
12. feature map 是否 data-dependent？
13. model selection 是否进入 inference？
14. numerical solver 是否稳定？
15. prediction、association 与 causal effect 是否分开？

## 二十五、小结

线性回归的完整逻辑链：

1. population risk 定义 best linear predictor；
2. normal equation 只给 feature-residual orthogonality；
3. correct conditional mean 给 \(E[U\mid X]=0\)；
4. fixed-design OLS 在 full rank 下有 closed form；
5. mean zero、homoskedasticity、Gaussianity分别承担 unbiasedness、simple covariance、exact inference；
6. Gauss–Markov 只证明 BLUE；
7. Random-X prediction error由 \(\Sigma\)-norm 衡量；
8. Gaussian random design 的 expected excess risk为 \(\sigma^2d/(n-d-1)\)；
9. heteroskedastic/misspecified settings 使用 sandwich asymptotics；
10. rank deficiency、ridge、numerical solver 与 learned features 改变不同层。

会写 \((X^\top X)^{-1}X^\top y\) 只是开始；真正掌握是知道每个 inverse、expectation 与 interval 依赖哪些条件。

## 来源与延伸

- [[S-2009-Hastie-Tibshirani-Friedman-ESL]]：linear methods、ridge、model assessment；
- [[S-1980-White-Heteroskedasticity-Covariance]]：heteroskedastic sandwich；
- [[S-2020-Rosset-Tibshirani-Fixed-Random-X]]：新输入 prediction error；
- [[稳定最小二乘与正规方程的风险]]：QR/SVD/normal-equation 数值边界；
- [[核岭回归与 Gaussian Process 接口]]：从 finite features 进入 RKHS 与 Bayesian function prior。
